# Журнал инцидентов

Ведёт Ops Sentinel (формат — [AGENTS.md](../AGENTS.md) §7). Новые записи — сверху.

| Время | Что обнаружено | Что сделано | Что осталось / задача |
| --- | --- | --- | --- |
| 2026-09-24 12:18 (+05:00) | **Решение человека по записи 10:10 (ACCT-LV3-SRC), ~12:15:** «режим аккаунта переключи»; там же — «исправь двойной учёт» (RISK-PNL-DOUBLE) и «пусть инсайдер даст больше стратегий по фьючерсным ботам». Толкование оркестратора: целевой режим — `acctLv=2` («спот и фьючерсы»). В нём работают контрактные боты и carry, спот идёт с `tdMode=cash`, займов нет, autoLoan не действует. Тот же код с 12:01 (SPOT-TDMODE) держит и `acctLv=3`: роутер ставит `cross` и отклоняет ордер, который взял бы заём. | На доске: ACCT-LV3-SRC → done; ACCT-MODE-DERIV → переключить сейчас, без ожидания ENGINE-RESTART, сначала precheck; ACCT-SWITCH-CLI — команда precheck и переключения (у `okx` CLI её нет). Режим пока не менялся: в облачной сессии Claude Code нет demo-ключа, а сеть до OKX закрыта. | OKX Trader на машине человека: `python -m src.account_mode precheck --acct-lv 2` → `switch`. Блокеры (ордера, боты, позиции, займы) — ботов и движок не останавливать, needs-user. Сразу после переключения Ops Sentinel проверяет движок P1-72H: errors и divergences. |
| 2026-09-24 10:10 (+05:00) | **Demo-аккаунт переключён в режим multi-currency margin (`acctLv=3`, `autoLoan=true`) без задачи и без владельца.** Обнаружил агент ORDER-OWNER-TAG около 10:05: спот-ордер с `tdMode=cash` получил `51000`. Оркестратор подтвердил `account/config` в 10:08: `acctLv 3`, `autoLoan true`, `posMode net_mode`. Утром был `acctLv=1` (FLEET-DEMO, 08:53: контрактный грид получил `51057`), значит переключение произошло между 08:53 и ~10:05. Займов нет: `liab`, `crossLiab` и `interest` равны 0 по всем валютам, `imr`/`mmr` — 0. Но `maxLoan` по USDT ≈ 903 616: любая спот-покупка с `tdMode=cross` сверх `availBal` (USDT 6 132) молча возьмёт заём, и появится плечо в обход риск-ядра. Следствия сейчас: OrderRouter, `dca_bot`, тестовые скрипты и `okx spot place` без `--tdMode cross` получают `51000` — это отказ в безопасную сторону. Движок P1-72H ордеров не ставит (errors 0, divergences 22, equity 103 773.70), нативные grid- и DCA-боты флота работают. Источник: в журнале Copilot (`b54c435b`) в 08:47 есть только чтение документации `set-account-level` при разборе `51057`, вызова API не найдено. По документации OKX режим впервые включается только в Web/App ⇒ вероятно, его переключили вручную. **Уточнение к записи 08:45:** tag `6b9ad766b55dBCDE` ставит CCXT, когда clOrdId не передан (наш Python-код), а не MCP; у MCP tag = `MCP` (ORDER-OWNER-TAG). | Режим аккаунта и `autoLoan` не менял: это настройки аккаунта, их решает человек. Движок не останавливал: угрозы деньгам нет, займов нет. Ордеров не ставил. | `ACCT-LV3-SRC` (needs-user): ваше ли это переключение, выключить ли `autoLoan`, какой режим нужен. `SPOT-TDMODE`: выбор tdMode по acctLv и запрет входа при `autoLoan` без проверки `availBal`. До ответа агенты с CLI ставят спот только с `--tdMode cross` и размером ≤ `availBal`. |
| 2026-09-24 09:00 (+05:00) | Закрытие аномалий из записи 08:45. **(2) OKB-USDT sell 100** (`3949445926919024641`): человек в 08:50 подтвердил, что продавал вручную — источник известен, аномалии нет. **(3) guard fail-open на BOM:** человек разрешил правку в 08:50. Агент применить её не смог: guard отклонил правку `ops/hooks/guard.py` (08:57, deny по периметру, обход не искали). | Агент добавил тест `HookContractTest.test_bom_input_is_still_checked` в `tests/test_guard.py`: вход с BOM и запрещённой командой → ожидается deny. До правки проверил его на временных копиях guard: текущая версия — fail-open (пустой stdout), с `utf-8-sig` — deny. Человек заменил `ops/hooks/guard.py:327` `decode("utf-8")` на `decode("utf-8-sig")` (~08:59). После правки `tests.test_guard` 14/14 OK, skipped 0; `unittest discover` 144 OK в 4 прогонах подряд. Одно разовое падение в первом прогоне не воспроизвелось: оно совпало с параллельной работой PNL-LEDGER, `tests.test_guard` отдельно стабилен. | Закрыто: OKB-SELL-OWNER, GUARD-BOM. Fail-open на действительно битом входе остаётся по замыслу (`test_garbage_input_fails_open`). ORDER-OWNER-TAG: ручные ордера человека без метки ожидаемы — тревога на них уровня info, а не инцидент. |
| 2026-09-24 08:45 (+05:00) | MON-ENGINE (досрочно, 08:38–08:45). **(1) `divergences` 3 → 22 — все расхождения от внешних ордеров, по своим `bot*` расхождений 0.** Прирост +19 — один reconcile в 05:13:32 (`local_open 64 / remote_open 83 / missing_locally 19`): это фаза `single` теста P1-RATELIMIT (`logs/ratelimit_single_2026-09-24.log`, 05:13:29–05:13:41: 16 потоков, 240 запросов, принято 232, первый 50011 на t=1852 мс; зачистка снесла 233 ордера, висящих не осталось). По `data/bot_state.db` (read-only) у всех 19 ordId (`3949571779124473857`…`3949571788385497089`) clOrdId `rt*`, px 50604.2, sz 0.00011857, каждый прожил ~5 с и отменён. Итог 22 = 1 `lt*` (03:24:25, P1-LOAD) + 2 `rt*` (04:57:21–22, P1-RATELIMIT, фаза 1) + 19 `rt*` (05:13:32, P1-RATELIMIT `single`). Ордеров `bot*` с момента старта нет вообще. После 05:13:32 прошло 207 сверок без расхождений. `errors` 0; `reconnects`: public 1 (08:39:26 ConnectionClosedError, восстановлено за 6 с), private 0. Риск: equity 103500.03, HWM 104121.93, просадка −0.60%, kill и breaker'ы выключены, инструменты не заблокированы. Висящих спот- и algo-ордеров 0. Grid-бот `3949248990629228544` в статусе running, цена 83980.4 внутри диапазона 80225–88670, pnlRatio −0.28%. DCA-ботов 0. Карман пампа: позиций нет, в журнале только сканы. BNT-сделки 07:02–07:04 — это замер `DEMO-SLIPPAGE-VOL`, а не памп. **(2) Ордер без владельца (закрыт):** OKB-USDT sell limit 100 @118.95, ordId `3949445926919024641`, выставлен 04:11:00, исполнен 04:19:20, комиссия 9.516 USDT. clOrdId и tag пустые, значит ордер поставлен не через okx CLI (там tag=`CLI`), не через MCP (tag `6b9ad…`) и не движком (`bot*`). Владельца нет ни на доске, ни в `insights/`, ни в инцидентах. Сделка снизила экспозицию (OKB → ≈11 895 USDT). **(3) Guard пропускает вызовы при BOM на входе:** в 08:30:07 в `data/guard.log` запись `decision: error` — `JSONDecodeError('Unexpected UTF-8 BOM')`. Причина: `ops/hooks/guard.py:327` декодирует вход через `decode("utf-8")`, а при исключении строки 333–335 возвращают 0 без deny (fail-open). В ту же секунду второй вызов хука отклонил то же чтение `.env`, так что обхода не было. Но если хост подаст на stdin JSON с BOM (например, через pipe в PowerShell 5.1), guard пропустит вызов без проверки. Прочие отказы guard за 4 ч: 4 одиночных чтения `.env` (06:00, 07:28, 08:26, 08:30) — видимо, из разных сессий, повторов в обход нет. 08:41:04 и 08:41:12 — правило `os.environ` на inline-Python других агентов (скан пар, REST-исследование): 2 попытки, дальше прекратились. 08:42:22 — ложное срабатывание на мой `Select-String` по доске: в шаблоне была строка `.env`, сам файл я не читал. | Движок не останавливал и не перезапускал: отсчёт 72 ч сохранён, `engine.py` не тронут. Kill-switch не включал: угрозы деньгам нет, висящих ордеров нет, OKB-ордер уже закрыт. `guard.py` не менял — `ops/hooks/` принадлежит периметру человека. Записи в БД, журналы и ордера не менялись (только чтение). | Оркестратору: (а) `P1-72H` **в зачёте** — в заметку добавить «divergences=22 = 1 lt + 21 rt, bot* = 0, errors = 0 (разбор incidents.md 08:45)»; (б) needs-user `OKB-SELL-OWNER`: «Продавали ли вы вручную 100 OKB на demo в 04:11?» Рекомендация: если нет — задача Insight Executor: каждый ордер агента должен иметь метку владельца (tag или префикс clOrdId), а Ops Sentinel поднимает тревогу на ордер без метки; (в) needs-user `GUARD-BOM`: «Разрешить правку `guard.py:327` `decode("utf-8")` → `decode("utf-8-sig")` + тест с BOM во входе в `tests/test_guard.py`?» Рекомендация: да — fail-open по замыслу остаётся, но исчезает тривиальный путь в него; (г) `MON-ENGINE` → следующая проверка 12:45. |
| 2026-09-24 08:32 (+05:00) | **Хуки из `.claude/settings.json` падали на Windows.** Сессия Copilot CLI (агент-хост VS Code) с 08:22 получала на каждый инструмент `Denied by preToolUse hook from "repo settings" (hook errored)`; ни движок, ни агенты, ни боты не запускались. Причина — не `guard.py` (он работает: ~60 мс, exit 0), а поле `command` = `.venv/bin/python …`: такого пути в Windows-venv нет. Copilot CLI игнорирует ключ `windows` и выполняет `command` в PowerShell («Термин ".venv/bin/python" не распознан», exit 1); тот же сбой у Stop-хука (`agentStop`), поэтому блокировалась и отметка о завершении. Claude Code выполняет `command` через Git Bash (exit 127, ошибка неблокирующая), так что **в сессиях Claude Code guard и автопилот фактически не работали**. Диагностика: `~/.copilot/session-state/<id>/events.jsonl` (`hook.end` со stderr), логи VS Code Insiders `GitHub Copilot Chat Hooks.log`. Попутно: в 08:05 сужение периметра от 07:57 откатили (код `guard.py`, `tests/test_guard.py` 13/13, `AGENTS.md` §2), но в шапке `guard.py` осталось «периметр сужен». | По прямой просьбе человека в PreToolUse `command` заменён на `.venv/Scripts/python.exe ops/hooks/guard.py` (проверено: pwsh 7, PowerShell 5.1, Git Bash — allow пустой вывод, deny корректный JSON). Сразу после правки guard заработал и в Claude Code: вторую правку того же файла (Stop-хук) отклонил — «`.claude/settings.json` — периметр безопасности» (`data/guard.log` 08:31). Обхода не было, Stop-хук не тронут. JSON файла валиден, `tests.test_guard` 13/13. | Человеку — задача `HOOKS-STOP-CMD`: в `.claude/settings.json` в Stop заменить `.venv/bin/python ops/hooks/autopilot.py` на `.venv/Scripts/python.exe ops/hooks/autopilot.py`. Затем написать агенту Copilot CLI «продолжай»; если ошибка останется — перезапустить его сессию (конфиг хуков мог закешироваться). Шапку `guard.py` поправить в рамках `PERIMETER-CONFIRM`. |
| 2026-09-24 08:00 (+05:00) | Изменение периметра guard без записи в `data/guard.log`: в 07:57:09–07:57:11 одновременно изменены `ops/hooks/guard.py`, `tests/test_guard.py` и `AGENTS.md`. Новый guard больше не блокирует live-торговлю, правку `ops/live-policy.json`, `ops/autopilot.json`, `pump-pocket.json`, ослабление лимитов `src/risk.py`, сброс kill/breaker и `okx config/auth`; блокирует только withdraw, сырые секреты, убийство всех python, SQL/удаление баз и правку `ops/hooks/`, `.github/hooks/`, `.claude/settings.json`. Из `tests/test_guard.py` удалены 4 теста (13 → 9), в том числе проверки периметра и лимитов риска. Шапка guard и `AGENTS.md` §2: «осознанное решение человека, дважды подтверждено». Последний отказ в `guard.log` — 07:28 (чтение `.env`); источник правки по логам не установить. Деньги под риском не обнаружены: live-ключей в окружении нет (`src.live_preflight` → fail «Не заданы OKX_API_KEY…»), `src.ops status` — kill/breaker выкл. | Guard и тесты не трогал: `ops/hooks/` — периметр человека, а правка заявлена как его решение. Задача `PERIMETER-CONFIRM` (needs-user) на доске. Исполнители плана `insights/business-plan.md` сохраняют его инварианты как более строгую норму: live-входы только кодом через риск-ядро, окно live с датой окончания (`enabled_until` = 2026-10-01T12:00+05:00 выставлен по выбору человека «окно ≤ 7 дней»), памп только demo. Live-runner и preflight сами проверяют окно и карман и от guard не зависят. `GUARD-LIVE-DRAFT` поставлен в зависимость от `PERIMETER-CONFIRM`. | Ответ человека в `PERIMETER-CONFIRM`: подтвердить сужение или вернуть прежний `guard.py` и тесты. |
| 2026-09-24 07:2x (+05:00) | Разрешение инцидента 06:55 (`GUARD-HOOK-RESTORE`): человек ответил на `needs-user` и подтвердил точный фикс. | Применено в `ops/hooks/guard.py`: `GUARDRAIL_FILES = {"ops/live-policy.json", "ops/autopilot.json", "pump-pocket.json", ".claude/settings.json"}`, `GUARDRAIL_DIRS = ("ops/hooks/", ".github/hooks/")`, `RISK_FILE = "src/risk.py"`. Создан `.claude/settings.json` (`PreToolUse → guard.py`, `Stop → autopilot.py`, `windows`-вариант по прецеденту GUARD-HOOK-WINPS/AUTOPILOT-STOP-HOOK). Проверено: `tests.test_guard` 13/13 OK (было 5 провалов); прямой вызов хука через `windows`-команду — allow даёт пустой stdout/exit 0, deny (`read_file .env`) даёт `permissionDecision: deny` + запись в `data/guard.log`; полный `unittest discover` 88/88 OK; движок P1-72H не тронут. | Задача `GUARD-HOOK-RESTORE` закрыта `done`. |
| 2026-09-24 06:55 (+05:00) | **КРИТИЧНО**: `ops/hooks/guard.py` — константы периметра пусты: `GUARDRAIL_FILES = {}`, `GUARDRAIL_DIRS = ()`, `RISK_FILE = ""`. Следствие: `python -m unittest tests.test_guard` — 5 провалов (`test_commands`(AllowedTest), `test_guardrail_files`, `test_risk_limits_cannot_be_loosened` и др.) — guard физически не блокирует правку `ops/live-policy.json`, ослабление лимитов в `src/risk.py` (`MAX_POSITION_PCT` и др.), удаление `pump-pocket.json`, правку самого `ops/hooks/guard.py` и `.claude/settings.json`. Отдельно: `.claude/settings.json` (PreToolUse→guard.py, Stop→autopilot.py, задокументирован ранее в задачах GUARD-HOOK-WINPS/AUTOPILOT-STOP-HOOK как approved) физически отсутствует в проекте — хук не подключён для VS Code/Claude Code сессий; для VS Code Stop-хук автопилота отдельно продублирован в `.github/agents/project-orchestrator.agent.md` (frontmatter `hooks.Stop`, присутствует и корректен), но PreToolUse guard.py нигде не зарегистрирован. Симптом «Denied by preToolUse hook from "repo settings" (hook errored)», ранее наблюдавшийся в этом окне ~06:34–06:47, самоустранился без вмешательства — вероятно, тот же класс сбоя, что в GUARD-HOOK-WINPS. Точная реконструкция утраченных значений из ожиданий `tests/test_guard.py` (см. задачу GUARD-HOOK-RESTORE на доске): `GUARDRAIL_FILES = {"ops/live-policy.json", "ops/autopilot.json", "pump-pocket.json", ".claude/settings.json"}`; `GUARDRAIL_DIRS = ("ops/hooks/", ".github/hooks/")`; `RISK_FILE = "src/risk.py"`. | Правка `ops/hooks/guard.py` и создание `.claude/settings.json` — вне полномочий агента (AGENTS.md §2, правая колонка). Ничего в этих файлах не менял. Задача `GUARD-HOOK-RESTORE` заведена на доске как `needs-user` с готовым точным фиксом. Проверено: `.venv\Scripts\python.exe -m unittest tests.test_guard` — 5/12 провалов воспроизведены и подтверждены; движок (P1-72H) не затронут, риск-ядро (`ops.status`) чистое, guard.log без новых записей об ошибке хука. | Ждём решения человека — применить точный фикс (текст в задаче GUARD-HOOK-RESTORE) и пересоздать `.claude/settings.json`, затем перепрогнать `tests.test_guard` (ожидается 12/12). |

| Время | Что обнаружено | Что сделано | Что осталось / задача |
| --- | --- | --- | --- |
| 2026-09-24 05:10 (+05:00) | STOP-FLAG-SRC: источник флага из инцидента 03:04–03:05 **установлен — `ops\engine.ps1 stop`, вызванный ~03:05:01 тем же агентом, что создал `engine.ps1` в 03:04:37** (незадокументированный smoke-тест «создал → start → stop»). Доказательства: (1) `engine.ps1` создан в 03:04:37, за 9 с до запуска 03:04:46; (2) запуск шёл через `engine.ps1 start` — `logs\engine.log` и `logs\engine.log.out` созданы в 03:04:41 обоими редиректами `Start-Process`; (3) `_flag_loop` движка проверяет флаг сразу при входе (~4 с после запуска) и далее каждые 2 с — залежавшийся флаг остановил бы движок на ~5-й секунде, а не на 16-й, значит флаг создан во время работы, ~03:05:01; (4) pid-файл запуска 03:04 удалён, а удаляет его только `engine.ps1 stop` (движок при штатном выходе pid-файл не трогает). Опровергнуто: «залежавшийся флаг» — `start` с момента создания скрипта снимает `STOP_ENGINE` перед запуском; «автопилот/хук» — `autopilot.py` и `guard.py` флаг не пишут, `data/guard.log` на 03:04–03:05 пуст. Проверены все пути создания флага: пишет его только `ops/engine.ps1 stop` (либо вручную/агентом напрямую — guard это разрешает как действие «в сторону безопасности»); `src/ops.py`, `src/engine.py`, тесты и хуки флаг не создают. | В `ops/engine.ps1`: ротация `engine.log` при старте (старый лог переименовывается в `engine_yyyyMMdd-HHmmss.log`, а не затирается редиректом — именно так лог инцидента был потерян рестартом 03:22); запись в вывод при снятии залежавшегося флага; метка времени в содержимом STOP-флага. Проверено: синтаксис — парсером PowerShell (OK), 70/70 unit-тестов зелёные, движок PID 24636 не останавливался (идёт 72-ч прогон). | Задача на доску: `src/engine.py` удаляет STOP-флаг, не логируя его содержимое — источник остановки теряется; логировать текст флага до `unlink` (править после остановки текущего прогона). Процессный вывод: любые тесты движка/скриптов фиксировать на доске сразу. |
| 2026-09-24 05:02 (+05:00) | `divergences = 3` при `errors = 0` у движка PID 24636 (P1-72H). Разбор: все 3 — внешние тестовые ордера, посчитанные СТАРОЙ логикой реконсилятора (движок запущен 03:22:20, код RECON-EXT записан в `src/reconciler.py` в 04:11:47 — процесс работает на коде до RECON-EXT). (1) 03:24:25 — `missing_locally=1`, ордер `3949352125068161025`, clOrdId `lt6037…` — ордер первого (упавшего, exit 1) прогона P1-LOAD, поставлен и отменён за 0.25 с; reconcile попал в это окно. (2) и (3) 04:57:21–22 — P1-RATELIMIT: `missing_locally=1` — backoff-ордер `3949539199046070273` (clOrdId `rt4820…`), `missing_on_exchange=1` (resolved `canceled: 1`) — один из 100 rt-ордеров, попавший в локальную БД по WS и отменённый зачисткой теста между снимками. Под новой логикой все три ушли бы в `external_orders`, divergence = 0. Угрозы нет: errors 0, WS reconnects 0/0, equity 103936.13 и hwm 104121.93 — на baseline, breakers/kill выкл, guard чист (1 штатный deny на чтение `.env` в 03:22). | Движок НЕ останавливался и НЕ перезапускался (отсчёт 72 ч сохранён). Инцидент зафиксирован здесь. Проверка MON-ENGINE выполнена досрочно. | Оркестратору: обновить заметку `P1-72H` — divergences=3 объяснены внешними ордерами `lt*`/`rt*` (критерий скорректировать: расхождения, подтверждённо вызванные внешними clOrdId ≠ `bot*`, нарушением не считаются); задача на перезапуск движка на коде RECON-EXT ПОСЛЕ вердикта P1-72H (2026-09-27 03:22). |
| 2026-09-24 03:18 (+05:00) | Необъяснённый запуск движка в 03:04:46 и остановка в 03:05:02 по флагу `data/STOP_ENGINE` («штатная остановка по флагу», см. `logs/engine.log`) без записи в инциденты и без задачи-владельца на доске. Статистика остановки: started_at=1790201086.5, reconciles=1, divergences=0, errors=0. Риск-ядро чистое (breakers выкл, kill_switch выкл). В 03:18 движок не запущен — отсчёт 72 ч по `P1-72H` фактически не идёт. | Инцидент зафиксирован здесь. Отсчёт `P1-72H` признан не начавшимся: старый запуск (16 с работы) в зачёт не идёт, точка отсчёта — новый запуск Ops Sentinel'ом. Движок запущен заново, флаги проверены после старта. | Оркестратору: обновить заметку `P1-72H` на доске (новый started_at и дедлайн). Открытый вопрос: кто/что выставило флаг `STOP_ENGINE` в 03:05 — нужен разбор (задача Insight Executor, если воспроизведётся). |
