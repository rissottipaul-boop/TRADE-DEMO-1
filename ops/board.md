# Доска задач

Единственный источник правды о текущей работе агентов. Правила — [AGENTS.md](../AGENTS.md) §3–4. Автопилот оркестратора читает таблицы машинно: не меняйте заголовки колонок; статус — первое слово ячейки.

Время — по часовому поясу проекта (+05:00).

## Активные

| ID | Задача | Агент | Статус | Зависит от | Критерий готовности | Заметки |
| --- | --- | --- | --- | --- | --- | --- |
| P1-72H | 72-часовой прогон движка на demo — критерий выхода Фазы 1 | Ops Sentinel | in-progress Ops Sentinel 03:20 | — | `ops\engine.ps1 status`: процесс жив ≥ 72 ч от `started_at`, `divergences == 0`, `errors == 0`; вердикт записан в `insights/phase1-progress.md` | Перезапуск движка обнуляет отсчёт — записать инцидент. Инцидент 03:04–03:05 зафиксирован (источник найден: smoke-тест engine.ps1, STOP-FLAG-SRC). Новый started_at 1790202140 (2026-09-24 03:22:20 +05), PID 24636; дедлайн 72 ч — 2026-09-27 03:22:20 +05. 05:02: divergences расследованы (incidents.md 05:02) — все вызваны внешними ордерами lt*/rt* нагрузочных тестов, движок на коде до RECON-EXT. Критерий скорректирован: расхождения по внешним clOrdId ≠ `bot*` нарушением не считаются; в зачёт — только свои ордера и errors 08:45 MON-ENGINE: divergences=22 = 1 lt + 21 rt, bot* = 0, errors = 0 (incidents.md 08:45) — в зачёте. |
| MON-ENGINE | Регулярная проверка движка, риска и guard-журнала | Ops Sentinel | scheduled 2026-09-24T12:45+05:00 | — | Проверки из роли Ops Sentinel выполнены, аномалии → `ops/incidents.md` и задачи на доске; задача переставлена на +4 ч | 08:45 (досрочно, divergences 3 → 22): все 22 внешние — 1 `lt*` (P1-LOAD 03:24) + 21 `rt*` (P1-RATELIMIT 04:57 и 05:13), `bot*` = 0, errors 0, после 05:13 — 207 сверок без расхождений; ws_public reconnect 1 (08:39, восстановление 6 с). Риск в норме (DD −0.60%). Аномалии: OKB-USDT sell 100 без tag/clOrdId в 04:11 (исполнен, снизил экспозицию) и fail-open guard на BOM в 08:30 — incidents.md 08:45. Следующая — 12:45. **Учесть с 10:00 (FLEET-KILL-DCA):** движок работает на коде от 03:22, его флаг `data/KILL` DCA-ботов не остановит — при аварии только `python -m src.ops kill`. **С 12:48 (RISK-PNL-DOUBLE, SPOT-TDMODE):** после ENGINE-RESTART проверять `risk.equity_age_s` ≤ 600 в `src.ops status`; режим аккаунта — `python -m src.account_mode status` (после ACCT-MODE-DERIV — `acctLv 2`, займа нет). **Не запускать `src.dca_demo_run` при работающем движке**, пока не закрыта DCA-EQUITY-SRC: ложный глобальный breaker |
| MON-GRID | Проверка grid-бота `3949248990629228544` | Ops Sentinel | scheduled 2026-09-24T10:30+05:00 | — | PnL, арбитражи, цена в диапазоне 80225–88670 — запись в `insights/grid-bot-demo-run.md`; при нарушении критерия остановки — действие по §6 того файла; задача переставлена на +6 ч | Регулярная до GRID-VERDICT |
| GRID-VERDICT | Вердикт по 48-часовому grid-прогону | OKX Trader | scheduled 2026-09-26T02:33+05:00 | — | Бот остановлен (`--stopType 2`), метрики и вердикт «своя сетка / нативный бот» в `insights/grid-bot-demo-run.md` | |
| P1-LOAD | Нагрузочный тест: 100+ place/cancel подряд | Insight Executor | done | — | Скрипт в `src/`, отчёт в `phase1-progress.md`: доля ошибок, задержки p50/p95, 0 висящих ордеров после теста | `src/load_test.py`; 100/100 циклов, ошибок 0%, p50/p95 place 255/261 мс, cancel 253/261 мс, висящих 0; лог `logs/load_test_2026-09-24.log`; 58/58 тестов OK |
| P1-WSDROP | Тест обрыва WS | Insight Executor | done | — | Unit-тест: разрыв → reconnect → login → повтор подписок → новый snapshot; живой прогон на demo с принудительным закрытием сокета; отчёт в `phase1-progress.md` | `tests/test_ws_reconnect.py` (4 теста, фейковый сервер на loopback) + живой прогон `src/ws_drop_demo_run.py` на demo 04:38: детект обрыва 250 мс, полное восстановление ~6 с (backoff 5 с), новый snapshot seqId 4213671→4214188, private login+resubscribe подтверждены, окно потерь ~6.2 с; дефект `websockets.exceptions` (AttributeError при subscribe до connect) исправлен в `ws_client.py`; 70/70 тестов OK; движок не затронут (errors 0, divergences без изменений); отчёт в `phase1-progress.md` |
| P1-RATELIMIT | Тест rate limit (50011 / 50061) | Insight Executor | done | — | Лимит контролируемо пойман на demo, backoff отработал, состояние не потеряно; числа лимитов — в `insights/okx-api.md` | `src/ratelimit_test.py` переписан: параллельный шторм + backoff под огнём; 50011 пойман (116 запр/<2с до отказа, token bucket ~60+30/с, пик ~189/2с), backoff с реальным ретраем reject_codes=[50011], висящих 0, 79/79 тестов; числа в okx-api.md §3, отчёт в phase1-progress.md; 50061 сознательно не провоцировался |
| ARCH-DEDUP | Консолидация дублей | Insight Executor | done | — | `src/state.py` и `src/ws_public.py` больше не используются (или удалены); `order_router` и `dca_bot` пишут в `storage.py`; колбэк kill-switch в `order_router` = `connector.emergency_stop`; свечи — через `ws_urls(...).business`; все тесты и самотесты зелёные | Удалены state.py, ws_public.py, ws_public_selftest.py; канон storage.py + ws_client.py; kill-switch → connector.emergency_stop; 58 тестов + самотесты 21/17/18 зелёные; state.db в корне не используется (удаление — STATEDB-DEL) |
| RISK-DCA-SLOT | Убрать хак `risk.record_pnl(inst, 0)` из `dca_bot` | Insight Executor | done | — | В `risk.py` явный API доливки спот-позиции; `dca_bot` не вызывает `record_pnl` на покупках; тест: 5 убытков подряд другой стратегии ставят системную паузу при работающем DCA | `risk.register_spot_buy(inst_id)`: отпускает слот risk_open_risk, не трогает серии убытков/day_pnl/equity; entries_today учитывается через register_entry роутера. `tests/test_risk_spot_buy.py` (5 тестов, вкл. паузу при работающем DCA); 63/63 unit, risk_selftest 21, dca_selftest 21, order_router_selftest 17 — зелёные. Контракт дописан в risk-core.md §6 |
| HUNT-BACKTEST | Дизайн бэктестера | Crypto Insight Hunter | done | — | `insights/backtester-design.md`: anti-lookahead, walk-forward, комиссии и проскальзывание OKX, формат данных, своя реализация или библиотека; ключевые решения — ≥ 2 источника | Решение: свой событийный движок ~300–500 строк поверх risk.py+SQLite; Freqtrade — эталон спецификации; taker 0.10%×2, slippage 5бп; walk-forward 180/30/30д + holdout 6 мес; данные history-candles → data/market_data.db |
| HUNT-OPEN-Q | Закрыть открытые вопросы в `insights/` | Crypto Insight Hunter | done | — | Каждый пункт «Открытые вопросы» в `okx-api.md`, `risk-core.md`, `spot-strategies.md`: ответ со ссылкой или отдельная задача на доске | 15 вопросов: 11 отвечены (5 эмпирически на demo), 4 вынесены в DEMO-SLIPPAGE, RISK-CALIB, KILL-FLATTEN-DEFAULT, RISK-FUNDING-PNL |
| STOP-FLAG-SRC | Разобраться, кто выставил флаг `data/STOP_ENGINE` в 03:05:02 24.09 | Insight Executor | done | — | Источник флага установлен (логи/хуки/скрипты) или зафиксировано «не воспроизводится» с проверкой всех путей создания флага в коде | Источник: `ops\engine.ps1 stop` при smoke-тесте свежесозданного скрипта в 03:05:01 (доказательства в incidents.md 05:10). Исправлено в engine.ps1: ротация engine.log при старте, метка времени в флаге; 70/70 тестов на момент проверки |
| RECON-EXT | Трактовка внешних ордеров в реконсиляторе | Insight Executor | done | — | Внешние ордера (без нашего `clOrdId`-префикса) не считаются divergence либо помечаются отдельной метрикой; тесты зелёные; задокументировано в `insights/okx-api.md` или `phase1-progress.md` | «Свой» = префикс `bot` (new_client_order_id); внешние → метрика `external_orders`, в БД не подбираются; 66/66 тестов OK (+3); задокументировано в `phase1-progress.md` |
| DEMO-SLIPPAGE | Измерить искажение исполнения market-ордеров в демо-стакане OKX | OKX Trader | done | — | `insights/demo-slippage.md`: ≥10 мелких market buy/sell BTC-USDT в demo, среднее/макс проскальзывание vs live-стакан, вердикт о пригодности демо-стакана | 12 ордеров, slip vs live mid среднее/макс 0.02/0.10 бп; вердикт: демо пригоден для мелких ордеров, бэктестеру дефолт 5 бп оставить; `insights/demo-slippage.md` |
| DEMO-SLIPPAGE-VOL | Замер проскальзывания в волатильную сессию | OKX Trader | done | — | `insights/demo-slippage.md` дополнен: замер в волатильную сессию (CPI/FOMC) или на менее ликвидном инструменте, сравнение со спокойным рынком | Активного CPI/FOMC-окна нет (CPI 11.09, FOMC 15–16.09, след. CPI 14.10) и BTC-USDT спред не расширен — выбран низколиквидный инструмент BNT-USDT (volCcy24h ≈$2.7k vs BTC ≈$725M, спред ≈80бп vs 0.012бп). 12 ордеров (6 циклов buy/sell по $15): slip vs live mid среднее/макс 40.34/42.62 бп (BTC-спокойный рынок: 0.02/0.10 бп) — разница ≈2000×, но объясняется полностью полуспредом BNT (все fill = touch), демо-искажения не найдено; 0 висящих ордеров, баланс перепроверен; вердикт: дефолт 5бп/сторону валиден только для BTC/ETH-тир, для низколиквидных альтов нужна spread-based модель; `insights/demo-slippage.md` §8 |
| DEMO-SLIPPAGE-SIZE | Size-dependent slippage в demo | OKX Trader | done | — | `insights/demo-slippage.md` дополнен: $50/$500/$2000 в demo vs walk-the-book расчёт по live books-400 | 12 ордеров: роста slippage с размером не обнаружено (макс 0.138 бп при $500, live WTB ≤0.011 бп); бэктестеру: ≤$5k без size-зависимости, >$5k — walk-the-book; `demo-slippage.md` §7 |
| RISK-CALIB | Калибровка порогов риск-ядра (MDD-breaker −15%, серии 3/5, cooldown 24ч, 10 входов/день) по статистике demo-торговли | Crypto Insight Hunter | ready | GRID-VERDICT, P1-72H | `insights/risk-core.md`: пороги подтверждены или скорректированы со ссылкой на статистику ≥7–14 дней | Закрывает вопросы 1, 2, 4 `insights/risk-core.md` |
| KILL-FLATTEN-DEFAULT | Дефолт flatten при kill-switch перед live | Human | done | — | Ответ зафиксирован в `insights/risk-core.md` §4 | Человек подтвердил 07:2x: `False` (оставить по стопам), совпадает с рекомендацией и текущей реализацией `kill_switch(flatten: bool = False, ...)` — код без изменений. Открытый вопрос §8 `risk-core.md` закрыт |
| RISK-FUNDING-PNL | Учёт funding rate в `record_pnl` для SWAP | Insight Executor | ready | BT-IMPL, RISK-PNL-DOUBLE | `risk_selftest`: record_pnl с funding меняет итоговый PnL; проверка на demo SWAP-позиции | Закрывает вопрос 5 `insights/risk-core.md`; спот-фазу не блокирует, источник funding — account/bills. 12:08 (оркестратор): зависимость от RISK-PNL-DOUBLE — обе задачи меняют смысл `record_pnl`, сначала решение по equity. **12:48 уточнение критерия по новому контракту:** funding попадает в equity через баланс биржи сам (`update_equity`). В `record_pnl` он влияет только на `day_pnl` и серии убытков: сделка SWAP закрывается с PnL, куда вошёл funding за время позиции. Тест — funding меняет `day_pnl` и итог серии, а не equity |
| BT-IMPL | Реализация бэктестера | Insight Executor | done | HUNT-BACKTEST | По дизайну из `backtester-design.md`; тесты anti-lookahead | Фаза 2. Дизайн утверждён: slicing/recursive-тесты зелёные, gap-check загрузчика, отчёт по метрикам §6.1 дизайна. Фазовая дисциплина (project-orchestrator.agent.md): реализация след. фазы не запускается до выхода Фазы 1 — старт не раньше вердикта P1-72H (дедлайн 2026-09-27T03:22+05:00) 08:38 решение оркестратора: зависимость P1-72H снята по прямому запросу человека 08:35 («запускай проект, нужно много прибыльных ботов»). Бэктестер — офлайн-код, engine.py и движок P1-72H не трогает, ордеров на бирже не ставит; без него прибыльность ни одной стратегии не доказать. Реализации стратегий в demo по-прежнему ждут P1-72H. Перехват 09:27: субагент Copilot упал в 09:03 (402). Есть `src/backtest/` (data, engine, metrics, walkforward, report, CLI) и тесты `tests/test_backtest_*` (зелёные); нет загруженных данных (`data/market_data.db`) и baseline-отчёта. **Готово 10:06** (принято Claude Code 11:36). Отчёт — `insights/backtest-baseline.md`: BTC-USDT и ETH-USDT, 1H с 2022-01-01, по 41 452 бара, дыр 0. В отчёте метрики §6.1, walk-forward 180/30/30 и гейт §6.2. Buy&hold и SMA-cross гейт не прошли — для проверки контура это ожидаемо. Anti-lookahead на реальных данных: slicing ✅, recursive ✅; holdout 182 дня не тронут. Тесты `tests/test_backtest_*` — 70 OK, `unittest discover` 284/284. **Находка:** `risk.record_pnl` учитывает PnL дважды, поэтому breaker −15% срабатывал уже при 9–11% реальной просадки → RISK-PNL-DOUBLE. Готовность бэктестера снимает зависимость у PUMP-CODIFY; MEANREV-IMPL и GRID-BT-SIM ещё ждут P1-72H |
| GRID-STRATEGY-DESIGN | Дизайн собственной grid-реализации (Стратегия №2 Фазы 2) | Crypto Insight Hunter | done | — | `insights/grid-strategy-design.md`: правило остановки всей сетки при выходе из диапазона, сайзинг шага/уровней, комиссии/проскальзывание, сравнение с нативным grid-ботом OKX (`grid-bot-demo-run.md`), критерии входа в бэктест/demo | `insights/grid-strategy-design.md`. Ключевое: Range-гейт входа (dca-bot-parameterizer классификатор); двусторонний hard stop с задержкой 30с/1 доп. шаг (аналог OKX triggerParams.delaySeconds); пол шага сетки поднят до 0.32% (round-trip maker fee 0.16% / 0.5) — поправка к trend-grid-bot skill, т.к. его 0.15–0.18% убыточны после комиссий; **находка по коду**: `order_router.py:229` шлёт каждый ордер через `register_entry` → глобальный `entries_today` (лимит 10/день, `risk.py:36`) — обслуживающие ордера сетки обязаны идти в обход, иначе сетка блокирует все стратегии за часы; `record_pnl` освобождает `risk_open_risk` безусловно → PnL агрегируется по сессии сетки (1 вызов при стопе), не по арбитражу; real-time просадка уже покрыта существующим `engine.py:205-217` → `risk.update_equity(totalEq)`. Источники ≥2 на ключевые решения (Bitsgap/Pionex/arXiv 2506.11921/Rebatly/MoneyFlock/Gainium/3Commas + внутренние инсайты и код). Новые задачи: GRID-BT-SIM, GRID-IMPL, GRID-OWN-DEMO (все — после P1-72H) |
| GRID-BT-SIM | Расширение бэктестера примитивом resting-limit-order (fill-if-touched) для path-dependent grid | Insight Executor | ready | BT-IMPL | По `insights/grid-strategy-design.md` §8.1–8.2: fill-if-touched с правилом «low раньше high», anti-lookahead тест (усечение ряда не меняет уже принятые филлы), fee-floor pre-check на этапе валидации конфига, тест изоляции entries/day (§6.1: ≥20 исполнений/день → счётчик входов растёт не более чем на 1), тест срабатывания hard stop на трендовом отрезке и non-triggering на боковике — все зелёные | Фаза 2. Фазовая дисциплина: старт не раньше вердикта P1-72H (дедлайн 2026-09-27T03:22+05:00) и готовности ядра бэктестера (BT-IMPL). **12:18 (оркестратор): зависимость от P1-72H снята по указанию человека «сделай всё, что есть в документации проекта»** — это офлайн-код бэктестера, как BT-IMPL. Правит `src/backtest/engine.py` — не параллельно с MEANREV-IMPL и PUMP-BT |
| GRID-IMPL | Реализация собственного grid-модуля `src/grid_bot.py` | Insight Executor | ready | GRID-BT-SIM, P1-72H | По `insights/grid-strategy-design.md` §2–6: Range-гейт входа, диапазон/шаг по формуле §4 (пол 0.32%), двусторонний hard stop §3 (cancel всех уровней + рыночная ликвидация + cooldown 4ч), обход `register_entry` для обслуживающих ордеров уровней (§6.1 — не более 1 инкремента `entries_today` за сессию сетки), агрегированный `record_pnl` по сессии (§6.2), сайзинг `quoteSz` через `size_position()` (§6.3); self-test зелёный; backtest-gate §8.4 пройден с отчётом | Фаза 2. Фазовая дисциплина: старт не раньше вердикта P1-72H |
| GRID-OWN-DEMO | Demo-прогон собственной grid-реализации ≥48ч и A/B с нативным ботом | Insight Executor | ready | GRID-IMPL | По `insights/grid-strategy-design.md` §7 и §9: метрики в том же формате, что `grid-bot-demo-run.md` (gridProfit/floatProfit-эквивалент, filledCount, время-в-диапазоне); 0 зависших ордеров; hard-stop либо корректно сработал, либо корректно не потребовался; `entries_today` не растёт быстрее 1/сессия; итог — сравнительная таблица своя/нативная в новом или обновлённом инсайте | Фаза 2. Требует прохождения GRID-IMPL |
| MEANREV-STRATEGY-DESIGN | Дизайн mean-reversion (RSI/BB) стратегии со стоп-лоссом (Стратегия №3 Фазы 2) | Crypto Insight Hunter | done | — | `insights/meanrev-strategy-design.md`: параметры RSI/BB, правило стоп-лосса и minimal ROI, anti-lookahead совместимость с `backtester-design.md`, ожидаемые метрики (Sharpe/win rate/avg trade) | Готово 2026-09-24 07:07. Артефакт: `insights/meanrev-strategy-design.md` — BTC-USDT спот 1H, RSI(14) crossed_above 30/70 + BB(20,2 typical price) + close-guard (long-only), стоп ATR(14)×1.5 (не фикс-%, привязан к `size_position()` risk-core.md), ROI-таблица с тайм-стопом 24ч/0%, 2+ источника на каждое ключевое решение (Wilder 1978, официальный Freqtrade `templates/sample_strategy.py`, Bollinger-дефолты, ATR-стоп практика, CoinQuant/Gate/LUT/SSRN бэктесты). Явный вывод §8: дефолты вероятно НЕ пройдут порог Sharpe≥0.5 backtester-design.md §6.2 без калибровки — не дефект, а честный прогноз. Открытые вопросы → задачи MEANREV-TEMA-CHECK и MEANREV-CALIB (ниже) |
| MEANREV-IMPL | Реализация mean-reversion (RSI/BB) стратегии | Insight Executor | ready | BT-IMPL, RISK-PNL-DOUBLE | По дизайну `insights/meanrev-strategy-design.md`: RSI(14)/BB(20,2 typical price) long-only сигналы, ATR(14)×1.5 стоп через `size_position()`, ROI-таблица §6.2 с тайм-стопом 24ч; прогон на бэктестере, отчёт по метрикам §6.1 backtester-design.md | Фаза 2. Фазовая дисциплина: реализация не раньше вердикта P1-72H (дедлайн 2026-09-27T03:22+05:00) и не раньше готовности бэктестера (BT-IMPL). **12:18 (оркестратор): по указанию человека «сделай всё, что есть в документации проекта» офлайн-часть стартует до вердикта P1-72H**, как BT-IMPL в 08:38: стратегия в бэктестере и отчёт, ордеров нет, движок не трогается. Demo-прогон стратегии — отдельной задачей после P1-72H. Ждёт RISK-PNL-DOUBLE: иначе бэктест занижен ложным breaker. Прогон на реальных данных — на машине человека (`data/market_data.db`). Правит `src/backtest/` — не параллельно с GRID-BT-SIM и PUMP-BT. ~12:55 субагент остановлен вместе с прерыванием хода человеком, правок не оставил; задача снова ready |
| MEANREV-CALIB | Калибровка порогов mean-reversion стратегии (ROI-таблица, ATR-множитель стопа, опционально ADX-регим-фильтр) | Crypto Insight Hunter | ready | MEANREV-IMPL | `insights/meanrev-strategy-design.md` §6.2/§7.2: числа подтверждены или скорректированы со ссылкой на walk-forward отчёт BT-IMPL; если пороги приёмки backtester-design.md §6.2 не пройдены на дефолтах — зафиксирован факт и первый шаг калибровки (напр. добавление ADX-фильтра) | Из §7.2, §8 meanrev-strategy-design.md: ожидаемый исход первого прогона — дефолты, вероятно, не проходят Sharpe OOS ≥0.5; по аналогии с RISK-CALIB |
| MEANREV-TEMA-CHECK | Сверить guard «close(t)>close(t-1)» vs TEMA(9) при порте стратегии во Freqtrade | Crypto Insight Hunter | ready | MEANREV-IMPL | Прогон обоих вариантов guard на одном датасете (наш движок vs порт во Freqtrade), сравнение числа сделок/expectancy — ответ в `insights/meanrev-strategy-design.md` §7.1 | Открытый вопрос §7.1 meanrev-strategy-design.md; не блокирует MEANREV-IMPL (можно стартовать с close-guard) |
| STATEDB-DEL | Удалить устаревший `state.db` из корня | Human | done | — | Файл удалён | Человек подтвердил 07:2x. Проверено перед удалением: `src/*.py` не ссылается на `state.db` (ARCH-DEDUP канонизировал storage.py+ws_client.py). Удалён; `unittest discover` 88/88 OK |
| KILL-CALLBACK-OWNER | Единый владелец регистрации kill-switch колбэка | Insight Executor | done | — | При одновременной работе engine и OrderRouter колбэк не перезаписывается: либо единый владелец (engine), либо registry с несколькими колбэками; тесты зелёные | Registry подписчиков в connector.py: register_kill_callback (идемпотентно, дедуп bound-методов), kill_switch_dispatch (изоляция исключений, слияние отчётов); order_router подписывается через реестр + close(); tests/test_kill_registry.py 10 тестов; 79/79 unit, risk_selftest 21, order_router_selftest 17 зелёные; контракт в risk-core.md §6. Ограничение: engine.py заморожен до конца P1-72H → ENGINE-KILL-REG |
| PUMP-SCAN | Скан рынка на пампы (demo) | Pump Risk Taker | scheduled 2026-09-24T10:40+05:00 | — | Отчёт скана, сделки только по правилам кармана, запись в `data/pump_journal.jsonl`; задача переставлена на +1 ч | Регулярная. Скан №5 09:47 (субагент Claude): топ-45 SPOT USDT без BTC и пар флота, 1H 03:00 UTC → 0 кандидатов. OKB-USDT +5.47% при объёме 39× медианы отклонён: RSI 72.8 выше окна 50–72, тот же базовый актив, что у DCA-бота флота OKB-BTC. LTC-USDT +1.02% — ниже порога 1.5%, RSI 73.7. Входов нет. Карман по правилам от 07:25 (10% equity): бюджет 10 362.91/10 362.91, дневной лимит 518.15/518.15 USDT. Журнал и `insights/pump-scan-2026-09-24.md` §«Скан №5». Следующий — 10:40. **12:35 (PUMP-CODIFY):** шаг 2 роли — команда `.venv\Scripts\python.exe -m src.pump_scanner --exclude ETH SOL SUI ADA TRX ETC APT BNB XLM DOT OKB` вместо ручного расчёта; строку `scan` в журнал пишет сканер; код 2 — входов в этом цикле нет. Живая проверка сканера — в PUMP-CODIFY |
| ALERTS-TG | Telegram-алерты: токен и чат | Human | needs-user | — | В `.env` есть `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` | Вопрос: создать бота у @BotFather и добавить токен и chat id в `.env`? Без этого агенты не могут сообщать об инцидентах вне VS Code. Рекомендация: да |
| ALERTS-IMPL | `src/notify.py` + алерты Sentinel | Insight Executor | ready | ALERTS-TG | Тестовое сообщение дошло; алерты на kill-switch, breaker, рассинхрон, падение движка, отказы guard | |
| PUMP-BUDGET | Размер рискового кармана | Human | done | — | Решение записано в `pump-pocket.json` | Человек выбрал 10% эквити (07:2x), переопределив рекомендацию исследования (5% до первых 20 сделок). `pump-pocket.json` пересчитан от текущей эквити (103629.11 USDT): `budget_usdt` 5190.88→10362.91, `daily_loss_limit` 259.54→518.15, `max_drawdown` 1557.26→3108.87, `max_risk_per_trade` 51.91→103.63, `max_position_pct` 519.09→1036.29 (внутренние доли 5/30/1/10% от budget сохранены); `insights/pump-scan-2026-09-24.md` обновлён |
| ERR-50011 | Добавить код 50011 в ERROR_MAP | Insight Executor | done | — | `explain_error("50011")` возвращает осмысленное действие (backoff), тесты зелёные | Из P1-RATELIMIT: сейчас fallback, есть только 50061. Проверено 06:56: код есть в `src/errors.py`, `tests/test_errors.py` 8/8 OK |
| GUARD-HOOK-RESTORE | КРИТИЧНО: `ops/hooks/guard.py` — `GUARDRAIL_FILES`/`GUARDRAIL_DIRS`/`RISK_FILE` пусты (guard не блокирует правку `live-policy.json`, ослабление лимитов `risk.py`, удаление `pump-pocket.json`); `.claude/settings.json` отсутствует — хук не подключён | Human | done | — | Значения восстановлены, `tests.test_guard` 12/12 зелёные, `.claude/settings.json` создан и хук проверен на реальном вызове | Человек подтвердил фикс 07:2x. Применено: `ops/hooks/guard.py` — `GUARDRAIL_FILES = {"ops/live-policy.json", "ops/autopilot.json", "pump-pocket.json", ".claude/settings.json"}`, `GUARDRAIL_DIRS = ("ops/hooks/", ".github/hooks/")`, `RISK_FILE = "src/risk.py"`; создан `.claude/settings.json` (`PreToolUse → guard.py`, `Stop → autopilot.py`, оба с `windows`-вариантом, по прецеденту GUARD-HOOK-WINPS/AUTOPILOT-STOP-HOOK). Проверено: `tests.test_guard` 13/13 OK; прямой вызов хука через `windows`-команду — allow (`python -m src.ops status`) даёт пустой stdout/exit 0, deny (`read_file .env`) даёт корректный `permissionDecision: deny` + запись в `data/guard.log`; полный `unittest discover` 88/88 OK |
| ENGINE-FLAG-LOG | Логировать содержимое STOP-флага до unlink в engine.py | Insight Executor | ready | P1-72H | В `_flag_loop` источник остановки пишется в лог; тесты зелёные | engine.py заморожен до конца 72-ч прогона (PID 24636) |
| ENGINE-KILL-REG | Перевести engine.py на register_kill_callback + kill_switch_dispatch | Insight Executor | ready | P1-72H | engine подписывается через реестр, слот risk получает диспетчер; тесты и самотесты зелёные | Из KILL-CALLBACK-OWNER; engine.py заморожен до конца P1-72H. Заодно docstring `engine.py`: kill останавливает «grid и DCA» (из FLEET-KILL-DCA) |
| ENGINE-STORAGE-CLOSE | Вызывать `self.db.close()` в `engine.stop()` | Insight Executor | ready | P1-72H | В `engine.py::stop()` после закрытия WS добавлен вызов `self.db.close()`; тесты и самотесты зелёные | Из STORAGE-CLOSE: `engine.py:50` создаёт `self.db = Storage()`, `stop()` (строки 111-116) закрывает только WS-сокеты, `Storage.close()` не вызывается. Сейчас это не утечка дескрипторов (Storage не держит персистентного соединения — каждая операция открывает/закрывает своё), но `close()` даёт WAL-чекпойнт (полезно перед остановкой движка) — engine.py заморожен до конца P1-72H |
| STORAGE-CLOSE | Storage.close() и закрытие sqlite-соединений в тестах | Insight Executor | done | — | ResourceWarning про sqlite в тестах устранён, 79+ тестов зелёные | Готово 06:5x. Корень предупреждения: `tests/test_storage_reconciler.py::test_migration_from_v0` открывал `with sqlite3.connect(path) as c:` — `__exit__` у `sqlite3.Connection` делает только commit/rollback, а НЕ закрывает соединение, отсюда `ResourceWarning: unclosed database` при сборке мусора. Сам `Storage` утечки не имел: каждая операция уже открывает и закрывает своё соединение через `_conn()` (проверено запуском только этого файла с `-W error::ResourceWarning` — до правок предупреждение всё равно всплывало при отложенном GC). Сделано: 1) `src/storage.py` — добавлен `Storage.close()` (идемпотентен, делает `PRAGMA wal_checkpoint(TRUNCATE)` — сливает WAL, снимает файловые локи `-wal/-shm` на Windows) + `__enter__`/`__exit__` для `with Storage(...) as db`; 2) `tests/test_storage_reconciler.py` — `c = sqlite3.connect(...)` с явным `try/finally: c.close()` вместо `with ... as c`; `StorageCase.tearDown` теперь вызывает `self.db.close()` перед удалением temp-директории; добавлен новый тест `test_close_is_idempotent_and_usable_as_context_manager`. Проверка: `.venv\Scripts\python.exe -W error::ResourceWarning -m unittest discover -s tests -t .` → 88 тестов, 0 ResourceWarning, 5 failures (все `tests.test_guard.*`, предсуществующие, не связаны со storage — трекаются отдельно в `GUARD-HOOK-RESTORE`, needs-user, вне полномочий — `ops/hooks/` не трогали). Самотесты `order_router_selftest` (17/17) и `dca_selftest` (21/21), использующие `Storage`, зелёные без изменений поведения. `engine.py` НЕ менялся (заморожен, P1-72H PID 24636 жив). Отдельная заметка: `engine.py:50` создаёт `self.db = Storage()` и не вызывает `.close()` при остановке (`stop()` закрывает только WS) — не утечка (Storage не держит персистентного соединения), но для полноты стоит завести задачу ENGINE-STORAGE-CLOSE на добавление `self.db.close()` в `engine.stop()` после разморозки P1-72H |
| ENGINE-RESTART | Перезапуск движка на коде RECON-EXT после вердикта P1-72H | Ops Sentinel | scheduled 2026-09-27T03:22+05:00 | P1-72H | Движок остановлен/запущен, новый started_at зафиксирован на доске, первая reconcile-сверка чистая | Заодно подтянутся правки engine.py и order_router.py, вошедшие после старта процесса; досрочно — только при расхождениях по своим ордерам bot* или росте errors. Подтянется и DCA-этап kill-switch (FLEET-KILL-DCA): до рестарта флаг `data/KILL` в движке DCA-ботов не останавливает, аварийно — `python -m src.ops kill`. **12:48 (RISK-PNL-DOUBLE):** рестарт переводит движок на новый контракт риск-ядра. После рестарта в `python -m src.ops status` значение `risk.equity_age_s` ≤ 600 (движок обновляет equity раз в 300 с). Иначе входы кода запрещены «equity устарела» — это инцидент |
| GUARD-HOOK-WINPS | PreToolUse-хук guard.py: "Denied ... (hook errored)" на каждый вызов инструмента в окне Project Orchestrator (VS Code/Windows) | Human | done | — | В `.claude/settings.json` у PreToolUse-хука добавлен `windows`-вариант команды; повторный вызов инструмента в окне Orchestrator проходит без "hook errored" | Причина: команда PreToolUse-хука рассчитана на Claude Code CLI ($CLAUDE_PROJECT_DIR + кавычки без `&`); VS Code выполняет её на Windows через PowerShell, где $CLAUDE_PROJECT_DIR не задаётся и кавычки без call-оператора — ошибка парсинга, guard.py вообще не стартовал (data/guard.log без новых записей). Человек добавил `"windows": ".venv\\Scripts\\python.exe ops\\hooks\\guard.py"` в `.claude/settings.json`. Проверено напрямую: тот же `windows`-хук прогнан через PowerShell с двумя входами — безобидное чтение каталога → exit 0, пустой вывод (allow); чтение `.env` → exit 0, корректный `permissionDecision: deny`, событие залогировано в `data/guard.log` (`ts 2026-09-24T01:00:33`). Оба пути (allow и deny) работают корректно через новую команду. Не проверено напрямую через сам UI окна Project Orchestrator — рекомендовано попросить его повторить последнее действие для финального подтверждения. |
| AUTOPILOT-STOP-HOOK | `ops/autopilot.json` (`enabled: true`) не даёт эффекта — Stop-хук `ops/hooks/autopilot.py` нигде не зарегистрирован | Human | done | — | В `.claude/settings.json` добавлен блок `"Stop"` (аналогично `"PreToolUse"`, с `windows`-вариантом команды на `autopilot.py`); после этого сессия реально блокируется при наличии ready-задач и `data/autopilot_state.json` создаётся | Обнаружено 2026-09-24 при разборе `autopilot.json`/`live-policy.json`: `.claude/settings.json` содержал только `"PreToolUse" → guard.py`. Человек добавил блок `"Stop" → autopilot.py` (windows-вариант). Проверено напрямую: `'{"session_id":"verify-stop-hook"}'` на stdin `.venv\Scripts\python.exe ops\hooks\autopilot.py` → `decision: block`, причина «готово задач: 1 (DEMO-SLIPPAGE-VOL)», exit 0, `data/autopilot_state.json` создан с записью сессии (count=1). Хук реально срабатывает через `windows`-команду. Тестовый файл состояния удалён после проверки. |
| AGENTS-LIVE-RULES | Инструкции агентов под малый live | Claude Code | done | — | OKX Trader и Pump Risk Taker не ставят live-входы из CLI даже при открытом окне; Ops Sentinel проверяет live-карман; строки `tools:` не тронуты | План — `insights/business-plan.md` (решение человека 24.09: малый live сейчас, $1–10k, сбалансированный профиль, copy trading). `okx-trader.agent.md`: в live только чтение и аварийные действия, входы — только `src.live_runner`; `pump-risk-taker.agent.md`: только demo до ≥ 20 demo-сделок и PUMP-CODIFY; `ops-sentinel.agent.md`: п. 6 проверки live-кармана + реакция «live-ордер не от runner → `src.ops kill --mode live`». Блокер №6 `business-plan.md` §3 |
| LIVE-SETUP | Шаги человека для малого live | Human | needs-user | — | Суб-аккаунт с бюджетом; ключ суб-аккаунта read + trade без withdraw в `.env` (`OKX_API_KEY/SECRET/PASSPHRASE`); `enabled_until` вместо `null` в `ops/live-policy.json`; `ops/live-pocket.json` по шаблону `business-plan.md` §5 | Вопрос: выполнить 5 шагов этапа 0 из `insights/business-plan.md` §4? Рекомендация: да — бюджет $500–1 000, окно до `2026-10-01T12:00:00+05:00`, live-профиль `okx` CLI не создавать. 08:00: шаг 3 сделал Claude Code (по вашему выбору «окно ≤ 7 дней»; с 07:57 AGENTS.md §2 разрешает агентам править live-политику) — `enabled_until` = `2026-10-01T12:00:00+05:00`. За вами — шаги 1, 2, 5 (суб-аккаунт и перевод, ключи в `.env`, Telegram) и числа для `ops/live-pocket.json` (шаблон `ops/live-pocket.example.json`) |
| HOOKS-STOP-CMD | Stop-хук автопилота: путь к Python для Windows | Human | done | — | В `.claude/settings.json` у Stop-хука `command` = `.venv/Scripts/python.exe ops/hooks/autopilot.py`; в сессии Copilot CLI отметка о завершении проходит без «hook errored» | **09:27 проверено (Claude Code):** человек заменил команду в 08:34 — в `.claude/settings.json` Stop = `.venv/Scripts/python.exe ops/hooks/autopilot.py`; в сессии Copilot CLI `b2536d2b` (08:50–09:04) `agentStop` ×4 и `preToolUse` ×144 — все success, «hook errored» нет. История: PreToolUse исправлен в 08:31 по вашей просьбе (`.venv/bin/python` → `.venv/Scripts/python.exe`), вторую строку guard заблокировал как периметр — это правка для вас. Вопрос: заменить в Stop `.venv/bin/python ops/hooks/autopilot.py` на `.venv/Scripts/python.exe ops/hooks/autopilot.py`? Рекомендация: да. Подробности — `ops/incidents.md` 08:32 |
| PERIMETER-CONFIRM | Подтвердить сужение периметра guard в 07:57 | Human | needs-user | — | Человек подтвердил или отклонил изменение; при отклонении — задача на восстановление периметра | В 07:57:09–07:57:11 одновременно изменены `ops/hooks/guard.py` (больше не блокирует live, правку `live-policy.json`/`autopilot.json`/`pump-pocket.json`, ослабление `src/risk.py`, сброс kill/breaker, `okx config/auth`), `tests/test_guard.py` (13 → 9 тестов: удалены проверки периметра и лимитов) и `AGENTS.md` §2 («осознанное решение человека, дважды подтверждено»). В `data/guard.log` отказов нет — кто применил правку, по логам не установить. Вопрос: это ваше решение? Рекомендация: если да — ответить «да», план `business-plan.md` сохраняет свои инварианты как более строгую норму; если нет — вернуть прежний guard.py и тесты. Запись — `ops/incidents.md` 08:00. **08:32:** в 08:05 сужение откатили — код `guard.py`, `tests/test_guard.py` (13/13) и `AGENTS.md` §2 снова в прежней редакции. Осталась шапка `guard.py` «периметр сужен», она противоречит коду. Уточнённый вопрос: подтверждаете откат и правите шапку (ваш файл, `ops/hooks/`)? Рекомендация: да |
| GUARD-LIVE-DRAFT | Подготовить патч guard под малый live | Insight Executor | ready | PERIMETER-CONFIRM | `insights/guard-live-patch.md`: готовый к вставке код для `ops/hooks/guard.py` и тесты для `tests/test_guard.py`: (1) `ops/live-pocket.json` в `GUARDRAIL_FILES`; (2) аварийные live-команды (`src.ops kill/status`, cancel, остановка ботов) разрешены и вне окна; (3) `OKX_MODE=live` — только для точек входа `src.live_*`; (4) нормализация путей `_norm_path` покрыта тестами для всех форм пути, которые передают инструменты VS Code и Claude Code | `ops/hooks/` агентам не править — только проект патча, применяет человек (GUARD-LIVE-PATCH). Блокеры №4–5 `business-plan.md` §3 |
| GUARD-LIVE-PATCH | Применить патч guard под малый live | Human | needs-user | GUARD-LIVE-DRAFT | Патч из `insights/guard-live-patch.md` применён, `tests.test_guard` зелёные | Вопрос: применить патч, когда он будет готов? Рекомендация: да — без него `ops/live-pocket.json` не защищён guard'ом, а аварийная live-команда с `OKX_MODE=live` вне окна блокируется |
| LIVE-STATE-SPLIT | Состояние по режимам demo/live | Claude Code | done | — | `src/config.py`: `data_dir(mode)` (live → `data/live/`, demo — прежние пути); `src/ops.py`: `status` и `kill` с `--mode live` работают на live-путях; тест: demo и live не делят risk- и storage-состояние; все unit-тесты и самотесты зелёные; P1-72H не затронут | `src/config.py`: `state_paths(mode)`/`data_dir`/`normalize_mode`/`default_mode`, `load_settings(mode=None)` — live-процессы передают `"live"` явно, `OKX_MODE` в `.env` остаётся demo. `src/ops.py`: `--mode` у подкоманд (`status/kill/reset --mode live`); опций перед командой нет намеренно — иначе `src.ops --mode live reset` обходил бы правило guard для reset; `kill` ставит `kill_active` и без live-ключей. `tests/test_live_state.py` 9 тестов. Проверено 07:53: `unittest discover` 97/97 OK, самотесты 21/17/21, `src.ops status` (demo) — started_at 1790202140, errors 0 — P1-72H не затронут |
| LIVE-PREFLIGHT | Предстартовая проверка live (только чтение) | Claude Code | done | LIVE-STATE-SPLIT | `src/live_preflight.py`: `account/config` — нет `withdraw` в `perm`, ключ суб-аккаунта (`uid ≠ mainUid`), `ip` (предупреждение до DEPLOY-VPS), equity ≤ `budget_usdt`, дрейф часов, `enabled_until` задан и не истёк, `ops/live-pocket.json` валиден; в live любой провал → exit 1, на demo — отчёт; unit-тесты на фейковых ответах биржи; прогон на demo | `src/live_policy.py` (окно без `enabled_until` = закрыто; валидация кармана; поддержан только рукав `dca`), `src/live_preflight.py`, шаблон `ops/live-pocket.example.json` (`example: true` — runner его не примет), `tests/test_live_preflight.py` 16 тестов. Поля `account/config` сверены с demo 07:55. Прогон 07:58: `--mode demo` → ok, exit 0; live → 6 fail (нет ключей, нет кармана, окно без даты) — live-запуск заблокирован до LIVE-SETUP. **Находки:** (1) demo-ключ отдаёт `perm=read_only,withdraw,trade` — вопреки отметке «без withdraw» в roadmap Фаза 0; для live preflight это fail; (2) `asset-valuation` USDT = 103903.7 (trading 103903.7, funding/earn/classic 0) при `account/balance totalEq` 103910.0 — поля для ботов нет, вопрос EQUITY-TOTAL реален. `unittest discover` 109 OK (13→9 в test_guard — см. PERIMETER-CONFIRM) |
| LIVE-RUNNER | Live-runner DCA-рукава + `ops/live.ps1` | Claude Code | done | LIVE-PREFLIGHT | `src/live_runner.py`: перед каждым входом перечитывает `ops/live-pocket.json` и `ops/live-policy.json` (окно закрыто или без даты окончания → входов нет); `risk.init(data/live/…)`, equity → `risk.update_equity`; ордера только через `OrderRouter`; `DCABot` в live только с лимитом `max_total_usdt`; флаг `data/live/KILL` → kill-switch; `ops\live.ps1` (start, status, stop); unit-тесты на фейковой бирже зелёные | `src/live_runner.py` (`--once` — одна покупка; коды 0/1 пауза/2 preflight/3 kill), `ops/live.ps1` (start только при зелёном preflight; pid/лог/флаги в `data\live\`, `logs\live_runner.log`; UTF-8 с BOM — работает и в PowerShell 5.1, `engine.ps1` без BOM — только pwsh 7). `src/dca_bot.py`: live только с `max_total_usdt` + `before_buy`, `equity_fn`, `sleep` (demo-поведение прежнее). **Найдено:** `DCABot` отдавал риск-ядру USDT-баланс — в кармане $1k после покупок на $150 сработал бы глобальный breaker −15%; в live equity = `asset-valuation` кармана (`live_preflight.fetch_pocket_equity`). Рестарт не даёт лишней покупки (интервал — от последней покупки в `data/live/bot_state.db`). `tests/test_live_runner.py` 8 тестов. Проверено 08:08: `unittest discover` 121/121 OK, самотесты 21/17/21, `ops\live.ps1 status` в pwsh 7 и 5.1; live-preflight 08:09 → exit 1 (нет ключей и кармана; окно ok) — запуск заблокирован до LIVE-SETUP |
| EQUITY-TOTAL | Equity риск-ядра при деньгах в ботах и Earn | OKX Trader | done | — | На demo проверено, входят ли средства нативного grid-бота (и Earn, если доступен в demo) в `totalEq` торгового счёта и в `asset-valuation`: числа до/после — в `insights/okx-api.md`; если не входят — задача Insight Executor на расчёт equity суммой по счетам до запуска grid или Earn в live | Выполнил Claude Code 08:12, только чтение, бот не тронут. **Spot grid — входит:** `curBaseSz`/`curQuoteSz` бота (0.002325518574 BTC / 203.6517 USDT) ровно равны `frozenBal` BTC/USDT торгового счёта, `eq = cashBal + frozenBal` → бот внутри `totalEq`, ложного breaker'а нет, код не нужен. **Earn** — отдельный счёт `earn` в `asset-valuation`, `totalEq` его не видит: live-runner уже берёт equity из `asset-valuation`; требование для движка — в ENGINE-LIVE-MODE. Purchase/redemption Earn в demo недоступны — проверка только в live (HUNT-EARN). Факты — `insights/okx-api.md` §10 п. 10–12 |
| LIVE-REVIEW | Еженедельный разбор live-кармана | Ops Sentinel | scheduled 2026-09-30T18:00+05:00 | — | `insights/live-review-<дата>.md` по шаблону `business-plan.md` §6, рекомендация человеку «продлить окно / не продлевать»; задача переставлена на +7 дней | За сутки до конца окна. Если live ещё не запущен — короткая запись и статус LIVE-SETUP |
| HUNT-CARRY | Исследование funding carry на OKX | Crypto Insight Hunter | done | — | `insights/funding-carry.md`: история funding BTC/ETH-USDT-SWAP (глубина API OKX + внешний источник), доходность по режимам рынка, издержки 4 ног (комиссии спот и своп), маржа и риск ликвидации шорт-ноги при 1x, порог рентабельности, работает ли funding в demo; ключевые факты — ≥ 2 источника | Рукав «Funding carry», `business-plan.md` §2.1. Перехват 09:27: субагент Copilot упал в 08:54 (402), файла нет. **Готово 09:52** (субагент Claude, принято оркестратором): `insights/funding-carry.md` validated, с оговоркой по режимам рынка. API OKX `funding-rate-history` отдаёт только 94 дня, и весь этот период бычий: BTC 5.75–6.4% годовых, ETH 4.1–5.9%. Четыре ноги стоят 0.20–0.30% за круг; окупаемость BTC 12–19 дней, ETH 18–26 (до 48). Ликвидация шорта 1x — при росте ≈ +99%. Капитал нужен ×2 ⇒ ≈ 2.1–3.2% на вложенное — не лучше Earn (≈ 3–3.5%, HUNT-EARN; сравнение в файле выровнено оркестратором). Funding в demo ≠ live ⇒ demo проверяет механику, не экономику. Вердикт: доля рукава 10–15%, не форсировать. Новые: CARRY-DEMO-ACCRUAL, HUNT-CARRY-DEEPHIST; критерий CARRY-IMPL уточнён |
| HUNT-EARN | Earn для простаивающего USDT | Crypto Insight Hunter | done | — | `insights/idle-cash-earn.md`: Simple Earn flexible и auto-earn — доступность для суб-аккаунта и в demo, ставка и её история, скорость выкупа, остаются ли средства доступны для ордеров и входят ли в `totalEq`; ≥ 2 источника | Рукав «Кэш (+Earn)»; связано с EQUITY-TOTAL. Claim (автопилот 08:50): пишет только `insights/idle-cash-earn.md`. Перехват 09:27: субагент Copilot упал в 08:54 (402), файла нет. **Готово 09:50** (субагент Claude, принято оркестратором): `insights/idle-cash-earn.md` validated, 12 источников. USDT Simple Earn Flexible ≈ 3.5% (30 дней: медиана 3.03%, диапазон 2.7–5.9%), история публично ≥ 1500 дней, выкуп мгновенный 24/7. Demo: savings отвечает 50038 (недоступно), `auto-earn status` и fixed-products работают. Simple Earn — отдельный счёт `earn`: вне `totalEq` и недоступен для ордеров; Trading Account Auto Earn(+) остаётся маржой, но только для VIP1–9. Экономика: карман $500–1 000 → $1.25–2.50/мес. Вывод: рукав разблокирован для ручной подписки человеком на Simple Earn Flexible, код-шедулер не строим. Новые задачи: ERR-50038 (сделано), HUNT-EARN-SUBACCT, HUNT-EARN-VIP. Перенос фактов в `okx-api.md` §10 и строка в `business-plan.md` §2.1 — после FLEET-KILL-DCA, ORDER-OWNER-TAG и PNL-LEDGER (они правят эти файлы) |
| HUNT-COPYTRADE | Условия copy trading OKX для лид-трейдера | Crypto Insight Hunter | done | — | `insights/copy-trading.md`: требования к лид-трейдеру (баланс, трек, KYC, суб-аккаунт), доля прибыли и выплата, какие инструменты копируются (SWAP, спот), API копитрейдинга и поля `roleType`/`traderInsts` в `account/config`, маркетплейс стратегий ботов как второй канал; ≥ 2 источника | Выполнил Claude Code 08:16. `insights/copy-trading.md` (validated): требования — KYC, > 500 USDT на торговом счёте, ≤ 500 buy/сутки; доля 8–13% чистой прибыли подписчиков, выплата раз в неделю, только без связанных позиций; копируются SWAP и **спот**, carry не копируется; **публичный список — только при lead assets ≥ 100 000 USDT** (объявление OKX 17.09.2025) ⇒ с $1–10k подписчики только по ссылке из своей аудитории; рынок по public API: топ-20 — медиана AUM ~$80k, 280 дней лидирования. Вывод — канал не раньше Q1 2027; `business-plan.md` §2.2 обновлён. Маркетплейс ботов и доля «до 30%» — в HUNT-COPYTRADE-2 |
| HUNT-COPYTRADE-2 | Уточнить открытые вопросы copy trading | Crypto Insight Hunter | done | — | `insights/copy-trading.md` §7: дата вступления порога 100 000 USDT; видимость метки «API trader» без списка; где действует доля «до 30%»; может ли лидом быть суб-аккаунт; доступность copy trading в регионе пользователя; маркетплейс стратегий grid-ботов как второй канал; ≥ 2 источника на факт | Не блокирует этапы 0–2 плана. Готово 09:03 (субагент Copilot упал на квоте сразу после записи файла; принято Claude Code 09:27): §7 — 5 из 6 пунктов закрыты, у каждого ≥ 2 источника. Порог 100 000 USDT действует с 29.09.2025 07:00 UTC. Метка «API trader» — фильтр внутри публичного списка: вне списка видимости не даёт. «До 30%» у Futures/Spot — только уровень Legend (≥ 500 000 USDT), а **сегмент Bot (лид-боты grid/DCA) — плоские 30% на любом уровне, от 500 USDT**; «маркетплейс ботов» — это и есть сегмент Bot. Список запрещённых регионов получен. Открыто: может ли лидом быть суб-аккаунт и регион KYC пользователя → COPYTRADE-UI-CHECK |
| UNIT-ECON | Затраты проекта в месяц | Human | needs-user | — | Цифры в `insights/business-plan.md` §1 | Вопрос: сколько в месяц уходит на подписки AI (Copilot, Claude), сервер и прочее? Без этого не посчитать точку безубыточности. Рекомендация: указать фактические суммы одной строкой |
| INCOME-OTHER | «Другое» среди источников дохода | Human | needs-user | — | Ответ в `insights/business-plan.md` §0 | Вопрос: что вы имели в виду под «Другое»? Варианты: Telegram-канал и партнёрская программа OKX; продажа разработки; свой вариант. Рекомендация: уточнить одной строкой |
| ENGINE-LIVE-MODE | Второй экземпляр движка для live | Insight Executor | ready | ENGINE-RESTART, LIVE-STATE-SPLIT | `src/engine.py` в режиме live работает со своими путями `data/live/` (БД, pid, `STOP_ENGINE`, `KILL`); demo- и live-движок не мешают друг другу; equity в live — из `asset-valuation` (`live_preflight.fetch_pocket_equity`), не `totalEq` торгового счёта (иначе перевод в Earn = ложная просадка, EQUITY-TOTAL); `ops\engine.ps1 -Mode live`; тесты зелёные | После разморозки engine.py (27.09). Закрывает остаток блокера №5 (общие флаги) |
| NATIVE-GRID-WRAP | Нативный grid-бот через код | Insight Executor | ready | GRID-VERDICT, EQUITY-TOTAL | `src/native_grid.py`: бот создаётся только при Range-гейте, шаг ≥ 0.32%, обязательный `slTriggerPx`, `quoteSz` ≤ лимита рукава из `ops/live-pocket.json`, `min-investment` перед созданием; надзор и остановка по `grid-strategy-design.md` §3; тесты зелёные; прогон на demo | Рукав «Нативный grid». В live бот создаёт только код, не CLI агента |
| PNL-LEDGER | Учёт PnL по рукавам + ежедневный отчёт | Insight Executor | ready | LIVE-RUNNER | Атрибуция по рукавам: сделки, комиссии, funding, earn; отчёт за день (файл + Telegram после ALERTS-IMPL); данные для LIVE-REVIEW; тесты зелёные | Основа решений о масштабе и трека для copy trading. Claim (автопилот 08:50): зависимость LIVE-RUNNER выполнена (done); пишет `src/pnl_ledger.py`(или аналог), `tests/`, документацию. Перехват 09:27: субагент Copilot упал в 08:54 (402) на старте, файлов нет. **Перехват 11:36 (Claude Code, облачная сессия).** Код уже есть: `src/pnl_ledger.py` (1 522 строки) и правила `ops/sleeves.json` (10:30). Не хватает тестов и методики `insights/pnl-ledger.md`, на которую ссылается модуль. Пишу `tests/test_pnl_ledger.py`, `insights/pnl-ledger.md` и строку в AGENTS.md §6. **11:49: тесты и методика готовы, осталась только проверка на demo.** `tests/test_pnl_ledger.py` — 52 теста на фейковой бирже: период +05:00, правила и реестр владельцев, атрибуция (бот, clOrdId, tag, funding, человек), средняя цена, тождество сверки, догрузка журнала с архивом, read-only к бирже и `bot_state.db`, отчёт md/json и текст для Telegram. `unittest discover` 336/336 OK, без ResourceWarning, самотесты 21/17/21. Методика — `insights/pnl-ledger.md`; строки в AGENTS.md §6; LIVE-REVIEW в `ops-sentinel.agent.md`. Правка в модуле: проценты по займу (bills type 7) раньше попадали в «ручное» с пометкой «вероятно, ордер человека». Теперь у них источник `interest:<ccy>`, а за период — предупреждение «на счёте был заём» (autoLoan, incidents.md 10:10). Осталось: прогнать `python -m src.pnl_ledger` на demo за 24.09 — файлы в `data/reports/`, невязка сверки, расхождение BTC-grid с `totalPnl` OKX; итог записать в `insights/pnl-ledger.md` §5 п. 5. Нужен demo-ключ, в облачной сессии его нет |
| PUMP-CODIFY | Детерминированный памп-сканер | Insight Executor | ready | BT-IMPL | `src/pump_scanner.py`: методика `spot-momentum-scan-validate` (закрытые 1H-свечи, импульс, vol_ratio к медиане, RSI) без LLM; бэктест; запуск по расписанию; на данных сканов из `insights/pump-scan-2026-09-24.md` результаты совпадают | Дешевле и надёжнее ежечасного LLM-скана; условие live-пампа. **12:08 (оркестратор, облачная сессия Claude Code): декомпозиция.** Здесь — сканер, режим повтора прошлого скана, тесты, документация и замена LLM-скана в PUMP-SCAN. Бэктест правил сканера — отдельная задача PUMP-BT. Исполнитель — субагент `insight-executor` (`.claude/agents/`). Пишет `src/pump_scanner.py` и `tests/test_pump_scanner.py` (новые), при нужде добавляет функцию в `src/backtest/indicators.py`, правит AGENTS.md §6 и шаг «Скан» в роли Pump Risk Taker. **12:35 Insight Executor (облако): код готов, живая проверка — на машине человека.** `src/pump_scanner.py`: только публичные GET `market/tickers`, `candles`, `history-candles` (другие запросы opener не пропускает, ключи не нужны). Лента `--feed demo` по умолчанию — заголовок `x-simulated-trading`, как `okx --demo` в сканах №1–5; `--feed live` — без него. Топ-45 USDT по `volCcy24h` без стейблов, BTC и `--exclude`; 99 закрытых 1H-свечей (`confirm=1`); импульс, vol× к медиане и среднему `volCcyQuote`, RSI Уайлдера, MA20, MACD 12/26/9, score скилла; пороги — параметры CLI; «почти кандидаты», «данных мало» (< 30), «свеча устарела». Повтор `--at` + `--pairs` — по history-candles, в журнал не пишет. Строка `scan` в `data/pump_journal.jsonl` (формат v1 — в docstring), коды выхода 0/2. Плюс `macd()` в `src/backtest/indicators.py`, `tests/test_pump_scanner.py` (47 тестов, 11 мутаций кода пойманы), AGENTS.md §6, шаг 2 роли Pump Risk Taker. Проверено: `unittest discover` 409/409 OK, `--help` работает. Расписание — регулярная PUMP-SCAN (+1 ч). Сети до OKX в облаке нет, живой прогон не делался. **Остаток (машина человека):** (1) повтор скана №5 — `.venv\Scripts\python.exe -m src.pump_scanner --at 2026-09-24T09:47+05:00 --pairs OKB-USDT LTC-USDT ONDO-USDT NEAR-USDT`. Ожидается: свеча 03:00–04:00 UTC, кандидатов 0; OKB-USDT +5.47%, vol× медиана ≈ 39.2 (среднее ≈ 0.73), RSI ≈ 72.8, >MA20 и MACD>0 — да → «почти: RSI вне 50–72»; LTC-USDT +1.02%, 11.5/10.0, RSI ≈ 73.7 → отклонён (импульс, RSI); ONDO +0.24%, 1.53/0.27, RSI 46.6; NEAR +0.16%, 0.10/0.03, RSI 47.6. Сравнивать числа (импульс ±0.01, RSI ±0.1), а не текст вердикта: в инсайте у ONDO «объём» — по среднему, по медиане 1.53 проходит; «24ч %» там из тикера. Не совпало — по очереди `--endpoint candles`, `--feed live`, `--bars 100`, в заметку — что совпало. (2) Живой скан `… -m src.pump_scanner --exclude ETH SOL SUI ADA TRX ETC APT BNB XLM DOT OKB` → код 0, 45 пар, +1 строка `scan` в журнале; код 2 на demo-ленте без ключа — задача. После (1) и (2) — `done` |
| PUMP-BT | Бэктест правил памп-сканера | Insight Executor | ready | PUMP-CODIFY | Правила `src/pump_scanner.py` (вход по сигналу закрытой 1H-свечи на открытии следующей, стоп 1.5–2.5% из `pump-pocket.json`, трейлинг или двухступенчатый тейк из роли Pump Risk Taker) прогнаны на истории топ-пар бэктестером `src/backtest` с anti-lookahead. Отчёт — число сделок, win rate, expectancy, PF, MDD, комиссии и проскальзывание по `backtester-design.md` §3 — в `insights/` | Из декомпозиции PUMP-CODIFY. Скорее всего, в движок бэктеста понадобится трейлинг-стоп: сейчас есть только стоп и тейк. Условие live-пампа по `business-plan.md` §2.1 — expectancy > 0 |
| PUMP-FEED | Лента данных для памп-сигналов: demo или live | Crypto Insight Hunter | ready | PUMP-CODIFY | Сравнение `python -m src.pump_scanner --feed demo` и `--feed live` за 24 ч на одном универсуме: доля плоских свечей без сделок, выбросы объёма, расхождение импульса и RSI. Ответ, влияет ли заголовок `x-simulated-trading` на публичные рыночные данные, — по ≥ 2 источникам. Рекомендация ленты по умолчанию для сканера и для PUMP-BT (история — live) записана в инсайт | Из PUMP-CODIFY 12:35. Demo-свечи строятся из сделок demo-счетов: бывают пустые плоские часы и выбросы объёма ×1000. Скилл okx-cex-market сам себе противоречит, влияет ли `--demo` на рыночные данные; `src/backtest/data.py` пишет «demo = live-книга». Нужна сеть до OKX |
| PUMP-LIQ | Проверка ликвидности кандидата кодом | Insight Executor | in-progress Insight Executor 13:07 | PUMP-CODIFY | Для кандидата сканера — оборот последней свечи и глубина стакана (`market/books`, публичный GET) в пределах заданного отклонения цены против лимита позиции `max_position_pct` из `pump-pocket.json` (только чтение). Кандидат без ликвидности получает отдельный статус с причиной. Unit-тесты на фейковых ответах зелёные | Из PUMP-CODIFY 12:35: сейчас ликвидность агент проверяет вручную. Скан №4: у IMX оборот ≈ 1.5k USDT/ч и стакан 2.2k USDT против позиции 1 036 USDT. ~12:55 субагент остановлен вместе с прерыванием хода человеком. Частичная правка `src/pump_scanner.py` сохранена коммитом `1b89d86`: стакан `market/books`, depth, spread и turnover против лимита кармана, `--no-liquidity`. Откатана коммитом `b7cc602`: тесты не дописаны, 6 тестов `test_pump_scanner` падали на `market/books`. Продолжать с `git checkout 1b89d86 -- src/pump_scanner.py`. 13:07 снова отдана субагенту insight-executor (модель sonnet): продолжить с `1b89d86`; файлы `src/pump_scanner.py`, `tests/test_pump_scanner.py` |
| PUMP-JOURNAL | Схема журнала кармана и расчёт остатков кодом | Insight Executor | ready | PUMP-CODIFY | Схема строк `entry`, `stop`, `exit`, `error` в `data/pump_journal.jsonl` (docstring и пример) рядом со строкой `scan` сканера. Функция и команда считают по журналу остаток бюджета кармана, дневной PnL и просадку против `pump-pocket.json` (только чтение). Роль Pump Risk Taker ссылается на команду. Unit-тесты зелёные | Из PUMP-CODIFY 12:35: формат событий, кроме `scan`, не описан, и остаток бюджета кармана считает LLM. ~12:55 субагент остановлен вместе с прерыванием хода человеком, правок не оставил; задача снова ready |
| PUMP-SCHED | Запуск памп-сканера по расписанию без LLM | Insight Executor | ready | PUMP-CODIFY | `ops/pump_scan.ps1` для планировщика Windows: ежечасный запуск `src.pump_scanner` через 1–2 мин после закрытия свечи, лог в `logs/`, код 2 → строка `error` в журнал. Команда регистрации задачи планировщика — в заметках. Синтаксис проверен парсером PowerShell | Из PUMP-CODIFY 12:35: критерий «запуск по расписанию» сейчас закрывает ежечасная PUMP-SCAN силами LLM-агента. Скрипт снимает LLM со сканов, решение о входе остаётся за Pump Risk Taker. Регистрацию в планировщике делает человек или агент на его машине |
| CARRY-IMPL | Модуль funding carry | Insight Executor | ready | HUNT-CARRY, RISK-FUNDING-PNL, HUNT-CARRY-DEEPHIST | `src/carry.py` по выводам `insights/funding-carry.md`: спот long + perp short 1x isolated, контроль дистанции до ликвидации, выход при отрицательном funding; тесты; 7 дней demo | Рукав «Funding carry». В live — после DEPLOY-VPS. Уточнение по HUNT-CARRY: funding в demo не совпадает с live, поэтому 7 дней demo проверяют только механику — две ноги, margin ratio, выход при отрицательном funding. Экономику (окупаемость 12–48 дней) оценивать по истории live funding; доля рукава — 10–15% |
| DEPLOY-VPS | Сервер 24/7 со статическим IP | Crypto Insight Hunter | done | — | `insights/deploy-vps.md`: требования (ОС, задержка до OKX, статический IP для IP-whitelist, автозапуск движка и runner, мониторинг, бэкап `data/`), 2–3 варианта с ценой, план переноса; покупка — отдельная задача `needs-user` | Обязательно до carry в live. Готово 08:57 (субагент Copilot; принято Claude Code 09:27): `insights/deploy-vps.md` validated — требования (статический IP под whitelist ключа до 20 IP, автозапуск Task Scheduler/systemd, мониторинг, бэкап `data/`), 4 провайдера с ценами (Vultr Tokyo + Windows ~$27–30/мес, Contabo Tokyo ~$6.3, Hetzner Singapore ~$7, DO Singapore $12), план переноса из 7 шагов; матчинг OKX переехал Гонконг → Токио (2 официальные страницы OKX + замеры Glassnode) ⇒ регион сервера — Токио. Покупка — VPS-BUY |
| TREND-DESIGN | Дизайн trend-following с ADX-режимом (запасная сигнальная) | Crypto Insight Hunter | ready | MEANREV-CALIB | `insights/trend-strategy-design.md` в формате `meanrev-strategy-design.md`: сигналы, стоп, режимный фильтр ADX (`spot-strategies.md` §4), пригодность для copy trading на SWAP | Если mean-reversion не пройдёт гейт (ожидаемо — §8 её дизайна) |
| FLEET-DEMO | Демо-флот нативных ботов OKX: 6–10 grid/DCA-ботов на разных парах для отбора прибыльных | OKX Trader | done | — | Боты запущены на demo; по каждому в `insights/bot-fleet-demo.md`: algoId, тип, пара, диапазон/шаг или шаг DCA, бюджет, стоп-условие, обоснование режима рынка; бюджет одного бота ≤ 5% demo-equity, суммарный худший убыток флота ≤ 6% equity (аналог `MAX_PORTFOLIO_HEAT_PCT`), у каждого бота SL/стоп; BTC-USDT не трогать (идут GRID-VERDICT и P1-72H); состояние подтверждено повторным запросом | Готово 08:53 (OKX Trader). 10 ботов running на demo — 7 spot grid (ETH, SOL, SUI, ADA, TRX, ETC, APT; шаг 0.34–0.56%, у всех SL) и 3 spot DCA (BNB, XLM, DOT; SL 13–19%). Инвестиции 10 275 USDT, макс. на бот 1 800 (1.74% equity); худший убыток флота 1 055 USDT = 1.02% equity (лимит 6%). Оркестратор перепроверил `okx --demo bot grid orders` и `bot dca orders --algoOrdType spot_dca` 08:55 — все 10 running, BTC-бот не тронут. Ограничитель — USDT (16.6k из 103.5k equity, остальное 1 BTC + 1 ETH), свободно 6 142. Не вышло: XRP/AVAX — 55301 (дисклеймер TradFi), контрактный грид — 51057 (acctLv=1), TON нет в demo. Артефакт `insights/bot-fleet-demo.md`, факты API — `okx-api.md` §10 п. 13–16 |
| MON-FLEET | Проверка демо-флота | Ops Sentinel | scheduled 2026-09-24T14:30+05:00 | FLEET-DEMO | По каждому боту PnL, APR, арбитражи, цена в диапазоне — таблица в `insights/bot-fleet-demo.md`; бот за стоп-условием остановлен; задача переставлена на +6 ч | Регулярная до FLEET-VERDICT |
| FLEET-VERDICT | Рейтинг демо-флота за 7 дней: кандидаты в live-рукава | OKX Trader | scheduled 2026-10-01T09:00+05:00 | FLEET-DEMO | Таблица PnL/APR/max DD/арбитражи/комиссии по каждому боту; вердикт keep/kill; проигравшие остановлены; кандидаты оформлены задачами для рукавов `business-plan.md` §2.1 (включение в live — только решение человека) | |
| OKB-SELL-OWNER | Чей ордер OKB-USDT sell 100 @118.95 (`3949445926919024641`, 04:11, без tag и clOrdId) | Human | done | — | Человек ответил; если не его — источник найден или закрыт ORDER-OWNER-TAG | 08:50 человек подтвердил: продавал вручную — аномалия закрыта, источник известен. Ордера человека без метки ожидаемы: тревога ORDER-OWNER-TAG должна различать «без метки» как info, а не инцидент, пока человек торгует на том же demo-счёте. Риска нет (incidents.md 08:45) |
| ORDER-OWNER-TAG | Метка владельца у каждого ордера агента + тревога на ордер без метки | Insight Executor | done | — | Все пути выставления ордеров кодом (`order_router`, `dca_bot`, `live_runner`, тестовые скрипты) ставят tag или префикс clOrdId владельца; правило для агентов с `okx` CLI (`--tag`/`--clOrdId`) записано в AGENTS.md §6; проверка «ордер без метки» в роли Ops Sentinel; тесты зелёные | Из MON-ENGINE 08:45 (OKB-SELL-OWNER). engine.py не трогать до P1-72H. Перехват 09:27: субагент Copilot упал в 08:54 (402), изменений в коде нет. **Готово ~10:05** (принято Claude Code 11:36, облачная сессия, по состоянию репозитория). Реестр `src/order_owner.py`: 15 владельцев, `python -m src.order_owner new <код>`. Аудит `src/order_audit.py`: коды выхода 0/1/2. Префикс владельца стоит во всех путях кода: `order_router` — `botr`, `dca_bot` — `botsdca`/`botldca`, `live_runner` — `botldca`, `dca_demo_run` — `bottdca`, `smoke_test` — `smk`, `load_test` — `lt`, `ratelimit_test` — `rt`; у движка по-прежнему `bot` (engine.py не трогали). Правило для CLI — AGENTS.md §6, проверка у Ops Sentinel — `ops-sentinel.agent.md` п. 3 и 6. Факты API — `okx-api.md` §10 п. 20–21, живой ордер `iex…` в 09:58. Правило действует с 10:00 (`RULE_SINCE`). Проверено 11:36: `unittest discover` 284/284 OK, `test_order_owner` + `test_order_audit` 39 OK, реестр валиден. Попутно найден режим acctLv=3 (incidents.md 10:10) → ACCT-LV3-SRC, SPOT-TDMODE |
| GUARD-BOM | guard.py fail-open на BOM во входе хука (`decode("utf-8")`, строка 327) | Human | done | — | В `ops/hooks/guard.py` `decode("utf-8-sig")`, тест с BOM в `tests/test_guard.py`, `tests.test_guard` зелёные | 08:50 человек разрешил замену; агенту guard правку `ops/hooks/` отклонил (08:57, обход не искали). ~08:59 человек сам заменил `guard.py:327` на `decode("utf-8-sig")`. Агент добавил тест `HookContractTest.test_bom_input_is_still_checked` (вход с BOM → deny). До правки на временных копиях guard: без правки — fail-open, с правкой — deny. После правки `tests.test_guard` 14/14 OK, skipped 0; `unittest discover` 144 OK ×4; условный skip из теста удалён. incidents.md 09:00 |
| FLEET-KILL-DCA | Kill-switch не останавливает DCA-ботов: добавить `spot_dca`/`contract_dca` в `connector.emergency_stop` | Insight Executor | done | — | `connector.emergency_stop` останавливает grid и DCA-ботов (spot и contract); unit-тест на фейковом exchange; проверка на demo на одном тестовом DCA-боте (создать → emergency-stop-путь только для него → state stopped), флот FLEET-DEMO не тронут | Из FLEET-DEMO: 3 DCA-бота флота сейчас переживут `src.ops kill` (ручная остановка — `insights/bot-fleet-demo.md` §7). Безопасность — приоритет 1. Перехват (Claude Code 09:27): субагент Copilot упал в 09:03 на квоте (402). Код уже есть: `connector.stop_dca_bots` (с фильтром `algo_ids`), DCA в `emergency_stop`, `tests/test_connector_kill_dca.py` — 14 тестов; `unittest discover` 198/198 OK. Осталось: проверка на demo на тестовом DCA-боте и документация. **Готово ~09:55** (субагент Claude, принято оркестратором 10:00). `stop_dca_bots`: сбой списка ботов теперь → `failed` (раньше уходил в `errors`, и kill рапортовал успех при работающих ботах); `51291` «бот уже остановлен» — не провал; ретраи сети и 50011/50001/50013 ×2 (1 с, 2 с) по `risk-core.md` §4 п. 5. Тестов kill-DCA 29 (было 14), в том числе сквозной путь `risk.kill_switch → kill_switch_dispatch → DCA` и `src.ops kill`. Demo: тестовый бот `3950110382492143616` (LTC-USDT, 10+10 USDT) создан в 09:41 и остановлен `stop_dca_bots(algo_ids=[…])` в 09:41:52 (`sCode 0`, в истории `stopped`); остаток 0.147 LTC (~10 USDT) — stopType 2 оставляет монеты. Оркестратор перепроверил в 10:00: `unittest discover` 257/257 OK; running — 4 DCA флота, 7 grid флота и BTC-grid, тестового бота нет. **Ограничение:** движок PID 24636 работает на коде от 03:22, поэтому до ENGINE-RESTART флаг `data/KILL` DCA-ботов не остановит; аварийная остановка — `python -m src.ops kill`. Документация: `bot-fleet-demo.md` §7 и §9 п. 3, `risk-core.md` §6, `okx-api.md` §10 п. 19. Новые задачи: FLEET-KILL-GRID-HARDEN, KILL-DOCS-DCA, FLEET-KILL-CDCA-DEMO |
| FLEET-EXPAND | Расширить флот без продажи BTC: гриды с котировкой в BTC/ETH (ETH-BTC и т.п.) | OKX Trader | done | FLEET-DEMO | Проверена ликвидность и спред X-BTC/X-ETH пар в demo; подходящие запущены по тем же лимитам (бот ≤ 5% equity, SL, худший убыток флота ≤ 6% equity), дописаны в `insights/bot-fleet-demo.md`; или записано, почему нельзя | Готово 09:0x (OKX Trader). Из 104 кросс-пар X-BTC/X-ETH в demo двусторонний стакан со спредом < 1% только у трёх; ETH-BTC стоит (80/99 часов без сделок, ATR 0.053%), SOL-ETH оторван от кросса на 289%, остальные пустые. Запущен 1 бот: spot DCA OKB-BTC `3950026152344997888` (0.01199 BTC ≈ 1 006 USDT, SL 10%, TP 1.5%, худший убыток 110.5 USDT) — оркестратор подтвердил running. Флот 11 ботов, ≈ 11 281 USDT (10.9% equity), худший убыток 1 165.9 USDT = 1.13% equity (лимит 6%); порог аварийной остановки флота −1 200 USDT. `insights/bot-fleet-demo.md` §10, `okx-api.md` §10 п. 17–18 |
| TRADFI-DISCLAIMER | Подтвердить дисклеймер TradFi Spot в demo-интерфейсе OKX | Human | needs-user | — | Бот на XRP-USDT создаётся на demo без 55301 | Вопрос: подтвердите в веб-интерфейсе OKX (режим demo) дисклеймер TradFi Spot — без него XRP-USDT и AVAX-USDT недоступны для ботов (код 55301). Рекомендация: да, это только demo |
| ACCT-MODE-DERIV | Перевести demo-аккаунт в `acctLv=2` («спот и фьючерсы») по решению человека | OKX Trader | ready | ACCT-SWITCH-CLI | `python -m src.account_mode precheck --acct-lv 2` без блокеров → `python -m src.account_mode switch --acct-lv 2` → `okx --demo account config`: `acctLv=2`. Сразу после — проверка Ops Sentinel: `ops\engine.ps1 status` (errors и divergences не выросли) и `python -m src.ops status`. Тестовый контрактный grid создаётся без 51057 и останавливается, флот не тронут. Итог — в `insights/okx-api.md` §10 | Из FLEET-DEMO (51057). Нужен для контрактных гридов и рукава carry. Не раньше вердикта P1-72H — смена режима влияет на движок. **10:10:** аккаунт уже в `acctLv=3` с `autoLoan=true`, но переключили его не по этой задаче (incidents.md 10:10). Итоговый режим — по ответу на ACCT-LV3-SRC; после него здесь остаётся только проверка: status движка чистый, контрактный grid создаётся без 51057. **12:18: решение человека «режим аккаунта переключи» (ACCT-LV3-SRC), цель — `acctLv=2`.** Зависимость от ENGINE-RESTART снята этим решением: переключать сейчас. Если precheck покажет блокеры (ордера, боты, позиции, займы), ботов, флот и движок НЕ останавливать — needs-user с перечнем блокеров. Риск для P1-72H: движок ордеров не ставит, но работает на коде от 03:22, поэтому сразу после переключения Ops Sentinel проверяет его метрики; рост errors — инцидент. У `okx` CLI команды смены режима нет — её делает ACCT-SWITCH-CLI. **12:48: команды и порядок** — строка ACCT-SWITCH-CLI и `okx-api.md` §10 п. 24; перед переключением записать исходные `errors` и `divergences` движка. **После перехода в `acctLv=2` спот через CLI ставится с `--tdMode cash`** (дефолт `okx` CLI). Указание из incidents.md 10:10 «только `--tdMode cross`» действовало для `acctLv=3`: в режиме 2 `cross` на споте — маржинальная сделка с возможным займом. `OrderRouter` перейдёт на `cash` сам в течение 5 мин (SPOT-TDMODE). Ответ `51070` — needs-user: первое включение режима 2 человек делает в Web/App (Demo trading → Settings → Account mode) |
| ACCT-SWITCH-CLI | Команда проверки и переключения режима demo-аккаунта | Insight Executor | done | — | В `src/account_mode.py`: `python -m src.account_mode precheck --acct-lv N` — GET `account/set-account-switch-precheck`, только чтение, вывод блокеров; `switch --acct-lv N` — только demo (live — отказ), сначала precheck, при блокерах выход 1 без переключения, затем POST `account/set-account-level` и повторное чтение `account/config` подтверждает `acctLv`. Unit-тесты на фейковой бирже; `unittest discover` зелёный | Для ACCT-MODE-DERIV. В CCXT 4.5.83 есть `private_get_account_set_account_switch_precheck` и `private_post_account_set_account_level` (проверено 12:18). Исполнитель — субагент `insight-executor`. **Готово 12:43, принято оркестратором 12:48.** Команды: `python -m src.account_mode status`, `precheck --acct-lv N`, `switch --acct-lv N`. precheck: «можно» — только при sCode 0 и пустом `unmatchedInfoCheck`; всё непонятное считается блокером; сырой ответ печатается. switch работает только на demo: при live или клиенте без demo-заголовка — отказ без сети, выход 2. Порядок: `account/config` (режим уже N → выход 0) → precheck (блокеры → выход 1, POST не уходит) → POST `set-account-level` → `account/config` до 3 раз (режим совпал → 0, нет → 1). Ошибки OKX и CCXT → выход 2. Поля ответа сверены по зеркалу документации OKX и по OKX.Net. Тесты: +25 (всего 51); `unittest discover` 457/457. Проверено вручную: `switch --mode live` → отказ, выход 2. Факты — `okx-api.md` §10 п. 24. На demo не запускалось: первый прогон — в ACCT-MODE-DERIV, возможен `51070` (первое включение режима — только в Web/App). |
| ERR-ACCT-SWITCH | Коды смены режима аккаунта в ERROR_MAP | Insight Executor | done | — | `explain_error` для `51070`, `59001`, `59132`–`59139` даёт осмысленное действие по `okx-api.md` §10 п. 24; `tests/test_errors.py` зелёные | Из ACCT-SWITCH-CLI 12:43: `src/errors.py` был вне той задачи. ~12:55 субагент остановлен вместе с прерыванием хода человеком, правок не оставил; задача снова ready. 13:07 снова отдана субагенту insight-executor (модель sonnet) вместе с ERR-BOT-CODES; файлы `src/errors.py`, `tests/test_errors.py`. **Готово 13:16, принято оркестратором.** `src/errors.py`: 10 кодов смены режима аккаунта (51070, 59001, 59132–59139) по `okx-api.md` §10 п. 24; у 51070 действие — needs-user: первое включение режима только в Web/App. `tests/test_errors.py`: +14 тестов на обе задачи, 18/18; `test_account_mode` 51/51; полный набор на чистой копии ветки OK. Устаревший комментарий к `SWITCH_HINTS` в `src/account_mode.py` поправлен |
| FLEET-USDT-CAP | Высвободить USDT под флот: продать часть BTC (≈ 0.3 BTC) после вердикта P1-72H | OKX Trader | ready | ENGINE-RESTART | Продано demo-BTC на сумму, достаточную для +5–8 ботов (≤ 30% BTC), ордер с тегом владельца; новые боты флота запущены по лимитам FLEET-DEMO и дописаны в `insights/bot-fleet-demo.md` | Из FLEET-DEMO/EXPAND: кросс-пары в demo мёртвые, USDT свободно 6 142. Ордер в BTC-USDT до ENGINE-RESTART запрещён (P1-72H, GRID-VERDICT) |
| VPS-BUY | Купить VPS 24/7 со статическим IP | Human | needs-user | DEPLOY-VPS | Сервер заказан, его IP записан в `insights/deploy-vps.md`; задача переноса для Insight Executor заведена | Вопрос: заказать Vultr Tokyo Cloud Compute 1 vCPU / 2 GB + Windows Server (~$27–30/мес)? Варианты: Contabo Tokyo Linux ~$6.3/мес (нужен перенос `.ps1` → systemd, 0.5–1 дня); отложить. Рекомендация: заказать Vultr Tokyo Windows после LIVE-SETUP — без live-ключа сервер пока не нужен, а этот вариант не требует правок кода (`insights/deploy-vps.md` §4, §8) |
| COPYTRADE-UI-CHECK | Copy trading: суб-аккаунт как лид и доступность в регионе | Human | needs-user | HUNT-COPYTRADE-2 | Ответ записан в `insights/copy-trading.md` §7 п. 4–5 | Вопрос: откройте в приложении OKX «Copy trading → Lead trader recruitment». Виден ли раздел под основным аккаунтом? Доступна ли заявка, если переключиться на суб-аккаунт (саму заявку не подавать)? Документация OKX на это прямо не отвечает (`copy-trading.md` §7). Рекомендация: проверить заодно с созданием суб-аккаунта по LIVE-SETUP — канал copy trading всё равно не раньше Q1 2027 |
| ERR-50038 | Код 50038 (функция недоступна в demo) в ERROR_MAP | Insight Executor | done | — | `explain_error("50038")` даёт действие «не ретраить», тест зелёный | Из HUNT-EARN. Сделал Claude Code 09:52: `src/errors.py`, раздел «Режим аккаунта»; тест `tests/test_errors.py::test_50038_demo_unavailable_is_not_retried`; `tests.test_errors` 9/9 OK |
| HUNT-EARN-SUBACCT | Simple Earn на live-суб-аккаунте (только чтение) | OKX Trader | ready | LIVE-SETUP | `okx earn savings balance --json` под ключом live-суб-аккаунта: код 0, 50038 или ошибка прав — однозначный ответ записан в `insights/idle-cash-earn.md` («Открытые вопросы», п. 2) | Из HUNT-EARN: документация OKX прямо не подтверждает Simple Earn Flexible для суб-аккаунта. Только чтение, без подписок и переводов |
| HUNT-EARN-VIP | Порог VIP для Trading Account Auto Earn(+) | Crypto Insight Hunter | scheduled 2026-10-01T10:00+05:00 | — | Порог VIP1 (активы или 30-дневный оборот) записан в `insights/idle-cash-earn.md` + вывод «достижимо / нет» для кармана | Из HUNT-EARN. Отложено оркестратором: при кармане $500–1 000 VIP недостижим, а лимиты сессий AI сейчас — узкое место; вернуться после первого LIVE-REVIEW (30.09) |
| FLEET-KILL-GRID-HARDEN | `stop_grid_bots`: те же дефекты, что были у DCA | Insight Executor | ready | — | В `connector.stop_grid_bots`: сбой списка → `failed`; «бот уже остановлен» — не провал; ретраи сети и 50011/50001/50013 ×2 (1 с, 2 с); пагинация `orders-algo-pending` по курсору (сейчас читается одна страница ≤ 100 ботов); фильтр `algo_ids`, как у DCA. Unit-тесты на фейковом exchange зелёные; проверка на demo на одном тестовом grid-боте через фильтр — флот и BTC-grid не тронуты | Из FLEET-KILL-DCA. Безопасность — приоритет 1. `src/connector.py` свободен: FLEET-KILL-DCA закрыта. **11:36 (Claude Code, облачная сессия): код и unit-тесты готовы, не хватает только проверки на demo.** В `connector.py` у grid и DCA теперь общая остановка `_stop_family`: сбой списка → `failed`, 51291 → `skipped`, повторы сети и 50011/50001/50013 ×2 (1 с, 2 с), курсор `after` по страницам, фильтр `algo_ids`. `tests/test_connector_kill_grid.py` — 23 теста, среди них регрессии «150 ботов за одну страницу» и «сбой списка = успех»; вместе с kill-DCA 52 OK, `unittest discover` 284/284. Осталось: тестовый grid-бот на demo → `stop_grid_bots(algo_ids=[…])` → повторным запросом подтвердить `stopped`, флот и BTC-grid не тронуты. Нужен demo-ключ: в облачной сессии нет `.env`, а сеть до www.okx.com закрыта политикой окружения |
| KILL-DOCS-DCA | Документация kill: «grid и DCA» | Insight Executor | done | ORDER-OWNER-TAG | Справка `--keep-bots` и docstring в `src/ops.py`, docstring `src/order_router.py`: kill останавливает grid- и DCA-ботов; тесты зелёные | Из FLEET-KILL-DCA. `engine.py` — в ENGINE-KILL-REG (заморожен до конца P1-72H). **Готово 11:40 (Claude Code).** Исправлены `src/ops.py` (docstring и справка `--keep-bots`), `src/order_router.py` (docstring модуля и `_cancel_all_orders`) и карта кода в AGENTS.md §6. Проверено: `python -m src.ops kill --help` → «не останавливать нативные grid- и DCA-боты OKX»; `unittest discover` 284/284 OK, `order_router_selftest` 17/17 |
| FLEET-KILL-CDCA-DEMO | Остановка contract_dca на demo | Insight Executor | ready | ACCT-MODE-DERIV | На demo создан один тестовый contract DCA-бот и остановлен через `stop_dca_bots(algo_ids=[…])`; итоговое состояние подтверждено повторным запросом, флот не тронут | Из FLEET-KILL-DCA: при acctLv = 1 контрактного бота не создать (51057), пока покрыто только unit-тестами |
| CARRY-DEMO-ACCRUAL | Начисление funding на demo SWAP-позиции | OKX Trader | ready | ACCT-MODE-DERIV | На demo открыта минимальная short-позиция 1x isolated ETH-USDT-SWAP; после ≥ 1 периода funding в `account/bills` есть запись funding, сумма ≈ rate × нотионал; позиция закрыта, висящих ордеров 0; итог — в `insights/funding-carry.md` §7 | Из HUNT-CARRY: без этого `record_pnl` с funding (RISK-FUNDING-PNL) нечем проверить на demo |
| HUNT-CARRY-DEEPHIST | История funding глубже 94 дней: медвежий 2022 и боковик 2023 | Crypto Insight Hunter | scheduled 2026-09-27T10:00+05:00 | — | `insights/funding-carry.md` §2.2: годовая доходность carry и доля отрицательных периодов BTC/ETH за 2022 и 2023 по количественному источнику с историей глубже 94 дней (например, Binance `fapi/v1/fundingRate` как прокси OKX или экспорт Coinglass); расхождение прокси с OKX на общем окне; ≥ 2 источника | Из HUNT-CARRY: другие режимы пока оценены только качественно. Отложено до выхода Фазы 1 — CARRY-IMPL всё равно ждёт RISK-FUNDING-PNL |
| ACCT-LV3-SRC | Режим demo-аккаунта: кто включил `acctLv=3` и `autoLoan` | Human | done | — | Ответ человека записан в `ops/incidents.md` (запись 10:10); итоговые режим и `autoLoan` подтверждены `okx --demo account config` | Из incidents.md 10:10. Между 08:53 и ~10:05 demo-аккаунт перевели в multi-currency margin: `acctLv=3`, `autoLoan=true`. Займов нет, но спот-покупка с `tdMode=cross` больше `availBal` возьмёт заём в обход риск-ядра. Вопрос: это вы переключили режим? Варианты: (а) выключить `autoLoan`, режим оставить; (б) вернуть `acctLv=1` (спот): код проекта заработает без правок, но контрактные боты и carry снова получат 51057; (в) оставить как есть. Рекомендация: (а) сейчас — пропадает путь к займу мимо риск-ядра, P1-72H это не затрагивает. Целевой режим выбрать вместе с ACCT-MODE-DERIV после вердикта P1-72H. **Ответ человека ~12:15: «режим аккаунта переключи»**; в том же сообщении — «пусть инсайдер даст больше стратегий по фьючерсным ботам». Толкование оркестратора: целевой режим — `acctLv=2` («спот и фьючерсы»). В нём доступны контрактные боты и carry, спот идёт с `tdMode=cash` без займов, autoLoan не действует. Ответ записан в incidents.md (12:18). Переключение и подтверждение режима через `account config` — ACCT-MODE-DERIV |
| SPOT-TDMODE | Спот-ордера кода при любом режиме аккаунта: tdMode по `acctLv`, без скрытых займов | Insight Executor | ready | — | `OrderRouter` (а через него `dca_bot` и `live_runner`) и тестовые скрипты (`smoke_test`, `load_test`, `ratelimit_test`) берут tdMode из `account/config`: acctLv 1–2 → `cash`, 3–4 → `cross`. При acctLv ≥ 3 и `autoLoan=true` покупка больше `availBal` котируемой валюты (продажа — больше `availBal` базы) отклоняется до биржи. `live_preflight`: `autoLoan=true` → fail. Unit-тесты на фейковой бирже зелёные. На demo спот-ордер кода проходит без `51000`, займа нет (`liab` = 0) | Из incidents.md 10:10 и `okx-api.md` §10 п. 22: сейчас все спот-ордера кода на demo получают `51000`, потому что CCXT по умолчанию шлёт `tdMode=cash`. Отказ безопасный, но стратегиям на demo нужен. `engine.place_order` не трогать до P1-72H — эта правка идёт в ENGINE-TDMODE после разморозки. Если человек вернёт `acctLv=1` (ACCT-LV3-SRC), задача остаётся: режим live-суб-аккаунта заранее неизвестен. **12:01 (Claude Code, облачная сессия): код и unit-тесты готовы, осталась проверка на demo.** Новый модуль `src/account_mode.py`: tdMode по `acctLv`, запрет займа сверх `availBal` при autoLoan или `enableSpotBorrow`. `OrderRouter` получил стадию `account_mode`: режим перечитывается раз в 5 мин, если его не прочитать — отказ. Скрипты `smoke_test`, `load_test`, `ratelimit_test` берут tdMode из режима аккаунта; у `ratelimit_test` потолок ордеров считается от `availBal`. `live_preflight`: при возможном займе проверка `spot_borrow` = fail. **Ловушка CCXT:** в режиме cross рыночная покупка без явного `cost` ушла бы как «0 USDT», поэтому роутер передаёт `tgtCcy=quote_ccy` и `cost` (`okx-api.md` §10 п. 23). `tests/test_account_mode.py` — 26 тестов, в том числе запрос настоящего CCXT без сети; `unittest discover` 362/362, самотесты 21/17/21. Фейки тестов получили `account/config`. Осталось на demo (нужен ключ): `python -m src.smoke_test` проходит без 51000; рыночная DCA-покупка на 5–10 USDT через `OrderRouter` тратит примерно эту сумму; `liab` = 0 |
| ENGINE-TDMODE | tdMode по `acctLv` в `engine.place_order` | Insight Executor | ready | SPOT-TDMODE, ENGINE-RESTART | `engine.place_order` берёт tdMode тем же способом, что и `OrderRouter` после SPOT-TDMODE; тесты и самотесты зелёные | engine.py заморожен до конца P1-72H |
| RISK-PNL-DOUBLE | Двойной учёт PnL в `risk.record_pnl` при внешнем источнике equity | Insight Executor | done | — | Ответ человека записан. При «да» Insight Executor делает фикс: equity и HWM пишет только `update_equity`; тест «прибыльная сделка + `update_equity` → HWM не завышен, breaker ровно на −15% реальной просадки»; тест «equity старше 10 мин → вход запрещён»; `risk_selftest` 3c/6c переписаны под баланс; контракт в `risk-core.md` §6 обновлён; константы лимитов не меняются | Из BT-IMPL (`backtest-baseline.md`, «Вывод»). `record_pnl` прибавляет PnL к equity, которую `update_equity` уже пересчитал по рынку. HWM завышается, и глобальный breaker срабатывает раньше номинала: в бэктесте при 9.11% и 10.69% реальной просадки. Проверка «свежести» equity не помогает: при equity по рынку PnL сделки уже был в ней как нереализованный, так что `record_pnl` не должен менять equity вообще. В бою `record_pnl` пока вызывают только бэктест и `risk_selftest`; первыми в бою его начнут вызывать MEANREV-IMPL и GRID-IMPL. Ошибка в безопасную сторону, поэтому любой фикс делает breaker менее «пугливым», чем сейчас, — по правилу асимметрии решает человек. **Вопрос: исправить?** Варианты: (а) да — equity и HWM ведёт только баланс биржи, `record_pnl` ведёт дневной PnL и серии; добавить запрет входов при equity старше 10 мин (иначе без фида не сработал бы −15%); (б) нет — оставить консервативный учёт и помнить о занижении в бэктестах и RISK-CALIB. **Рекомендация: (а)**, до MEANREV-IMPL и GRID-IMPL. Лимит 15% не меняется, breaker возвращается на заданный порог. RISK-CALIB калибровать уже после фикса. **~12:15 решение человека: «исправь двойной учёт» — вариант (а).** Исполнитель — субагент `insight-executor`. **Готово 12:44, принято оркестратором 12:48.** `risk.py`: equity и HWM пишет только `update_equity`. `record_pnl` ведёт day_pnl, серии, блокировки, паузу и дневной лимит по day_pnl; глобальный breaker проверяет по equity из `update_equity`. Вход запрещён, если equity не выставлялась или старше `EQUITY_MAX_AGE_S = 600` с. Отметка `equity_updated_at` хранится в risk_kv и переживает рестарт; NaN и inf отбрасываются; `status()` показывает `equity_age_s`. Константы лимитов сверены с началом дня (`cb70fd8`): 12 прежних, добавлена только `EQUITY_MAX_AGE_S`. `risk_sim.py` вызывает `update_equity` на каждом баре. `tests/test_risk_equity_source.py` — 23 теста; на старом коде 18 из 22 красные. `risk_selftest` 3c/6c переписаны под баланс, добавлены 3d, 6d, 7a–c — всего 26. `test_live_state`: kill-тест со свежим equity. Синтетика: breaker раньше срабатывал при 14.53% и 14.89% просадки, теперь при 15.006%. Приёмка: `unittest discover` 457/457 (с `-W error::ResourceWarning`), самотесты 26/17/21, `engine.py` не тронут. Контракт — `risk-core.md` §6, пометка в `backtest-baseline.md`. Осталось: BT-BASELINE-RERUN; движок перейдёт на новый контракт после ENGINE-RESTART. До этого процессы на новом коде без своего `update_equity` получают отказ «equity ни разу не выставлялась» — это отказ в безопасную сторону. |
| DCA-EQUITY-SRC | Один источник equity для demo-риска: DCABot против движка | Insight Executor | ready | — | Процессы demo, которые пишут в общий `data/risk_state.db`, отдают риск-ядру одну и ту же величину equity — `totalEq` счёта, а не USDT-баланс. Тест: движок (HWM ≈ 104k) и `DCABot` на одном состоянии → breaker не срабатывает. `dca_selftest` и unit-тесты зелёные | Из RISK-PNL-DOUBLE 12:44, дефект был и раньше. `DCABot` в demo по умолчанию отдаёт риск-ядру USDT-баланс, а движок — `totalEq`. На фейке HWM 104 000 от движка и USDT 16 600 от бота дают глобальный breaker при «просадке» −84%. **До исправления `python -m src.dca_demo_run` при работающем движке не запускать: снять breaker может только человек**. ~12:55 субагент остановлен вместе с прерыванием хода человеком, правок не оставил; задача снова ready |
| ROUTER-EXIT | `OrderRouter`: выход из позиции — не новый вход | Insight Executor | ready | — | `OrderRouter.place_order` различает вход и выход (флаг или отдельный метод). Выход не проверяется через `check_entry_allowed` (хедж, лимит входов, breaker'ы, свежесть equity) и снимает слот риска. Тесты: продажа после покупки через роутер проходит; выход при глобальном breaker и при устаревшем equity проходит, вход — нет. Самотесты зелёные | Из RISK-PNL-DOUBLE 12:44: сейчас продажа ETH/USDT после покупки через роутер отклоняется как «хедж запрещён», любая продажа при глобальном breaker тоже. У `engine.place_order` вход и выход уже различаются. Нужна до demo-прогонов MEANREV и GRID. ~12:55 субагент остановлен вместе с прерыванием хода человеком, правок не оставил; задача снова ready |
| BT-BASELINE-RERUN | Перепрогон baseline бэктеста после RISK-PNL-DOUBLE | Insight Executor | ready | RISK-PNL-DOUBLE | Команда из шапки `insights/backtest-baseline.md` на тех же dataset_id. В «Выводе» нет строки о срабатывании breaker'а раньше номинала; при срабатывании просадка ≥ 15%; отчёт перезаписан | Выполнять на машине с `data/market_data.db`: в облачной сессии нет ни данных, ни сети до OKX |
| RISK-PNL-DOCS | Документация старого контракта `record_pnl` | Insight Executor | ready | RISK-PNL-DOUBLE | Старый контракт «record_pnl меняет equity и HWM» убран из `insights/backtester-design.md` §1 п. 5, `insights/grid-strategy-design.md` §6.2, `src/backtest/__main__.py` (текст причины срабатывания breaker'а в отчёте — нейтральный, с допуском на ровно −15%), docstring `report.breaker_trips` и `src/dca_bot.py`. Тесты зелёные | Из RISK-PNL-DOUBLE 12:44 — эти файлы были вне списка той задачи |
| GUARD-EQUITY-AGE | Защитить `EQUITY_MAX_AGE_S` в guard | Human | done | RISK-PNL-DOUBLE | `EQUITY_MAX_AGE_S` добавлена в `RISK_LIMITS_UP_IS_RISKIER` в `ops/hooks/guard.py`; `tests.test_guard` зелёные | Вопрос был: добавить новую константу риск-ядра в список лимитов, которые guard запрещает ослаблять? **Ответ человека ~12:55: «добавить новый порог «equity не старше 10 минут»».** Правку `ops/hooks/guard.py` по прямому указанию человека внёс оркестратор: `EQUITY_MAX_AGE_S` в `RISK_LIMITS_UP_IS_RISKIER`. Тест `DeniedTest.test_equity_max_age_cannot_be_loosened`: 600 → 3600, `float("inf")` и `risk.EQUITY_MAX_AGE_S = 10**9` из другого модуля — deny; 600 → 300 и прежние 600 — allow. Контроль: guard без правки пропускал все три ослабления. `tests.test_guard` 15/15 OK. `risk-core.md` §6 дополнен |
| PNL-DAILY | Ежедневный отчёт PnL по рукавам (demo; live — после запуска кармана) | Ops Sentinel | scheduled 2026-09-25T00:15+05:00 | PNL-LEDGER | Отчёт за вчера: `python -m src.pnl_ledger --date <вчера>`, при запущенном кармане — ещё и с `--mode live`; файлы в `data/reports/`. Код выхода 1 — предупреждения разобраны: инцидент или задача на доске. Задача переставлена на +1 день | Из PNL-LEDGER: «ежедневный отчёт» критерия. Одна команда, только чтение. Пока нет ALERTS-IMPL, текст для Telegram (`--telegram`) — в заметке задачи |
| HUNT-FUTURES-BOTS | Стратегии фьючерсных (контрактных) ботов OKX для demo-флота и рукавов | Crypto Insight Hunter | done | — | `insights/futures-bots.md`: не меньше 5 стратегий контрактных ботов OKX (контрактный grid long, short, neutral; контрактный DCA long и short; сигнальный бот и другие). Для каждой — механика, параметры API (`algoOrdType`, `lever`, `direction`, SL/TP), подходящий режим рынка, сайзинг в лимитах проекта (плечо ≤ 3x isolated — `business-plan.md` §7; бот ≤ 5% equity; худший убыток флота ≤ 6%), дистанция до ликвидации, ожидаемая доходность и риски. Ключевые факты — ≥ 2 источника. Предложены строки доски для запуска на demo и для кода | Решение человека ~12:15: «пусть инсайдер даст больше стратегий по фьючерсным ботам и можно их тоже вносить». Запуск на demo — после ACCT-MODE-DERIV: в `acctLv=1` контрактные боты получают 51057, в `acctLv=2` доступны. Исполнитель — субагент `crypto-insight-hunter`; предложенные задачи вносит оркестратор. **Готово к 12:50 (WIP `fb1325e`), принято оркестратором 12:51.** `insights/futures-bots.md`: 10 стратегий С1–С10 — механика, параметры API, режим рынка, сайзинг, ликвидация, funding и ADL, пригодность для copy trading. Кандидаты для первой волны: BTC-USDT-SWAP — нейтральный грид 2x, ETH-USDT-SWAP — DCA long ≤ 2x, DOGE-USDT-SWAP — short-грид 1x. Лимиты: плечо ≤ 2x, маржа бота ≤ 2% equity, сумма маржи ≤ 4%, сумма плановых худших убытков ≤ 0.8%. Факты — ≥ 2 источника (§8.1), 12 вопросов для demo (§9) стали задачами. **Главные находки:** (1) kill со `stopType 2` оставляет контрактную позицию с плечом без SL → KILL-CONTRACT-STOPTYPE; (2) неизвестно, входят ли деньги контрактного бота в `totalEq`, — без этого возможен ложный дневной breaker → сначала CFLEET-PROBE одним ботом; (3) maker на SWAP 0.02% против 0.08% на споте. Доходность не доказана |
| CFLEET-PROBE | Проба контрактного бота на demo: equity, ликвидация, остановка | OKX Trader | ready | ACCT-MODE-DERIV | Один нейтральный contract grid BTC-USDT-SWAP 2x, `sz` ≈ `min-investment`, `algoClOrdId` владельца `trd`. До и после создания записаны `totalEq`, `asset-valuation`, bills (type 12, 8). `details` (eq, availEq, actualLever, liqPx, fundingFee) и `positions` (mgnMode, liqPx, adl) сверены с `liquidate-price`. Проверено: видна ли позиция в `account/positions`; открыта ли позиция на старте при `basePos=true` от CLI; что остаётся после `stopType 2`; закрытие `stopType 1`; цена SWAP на demo против live. Итог — в `okx-api.md` §10 и `futures-bots.md` §11; флот не тронут | Из HUNT-FUTURES-BOTS: ответы на §9 п. 1–5, 7, 8, 10 `futures-bots.md`; гейт для CFLEET-DEMO и EQUITY-CBOTS. Если kill остановит пробный бот, позицию не закрывать (KILL-CONTRACT-STOPTYPE); дальше — правило §6.5 `futures-bots.md`. Бот останавливать со `stopType 1` в конце пробы |
| KILL-CONTRACT-STOPTYPE | stopType контрактных ботов при kill-switch | Human | done | — | Ответ записан в `risk-core.md` §8; при «да» — задача Insight Executor: stopType по типу бота в `connector.stop_grid_bots` и `stop_dca_bots` | Из HUNT-FUTURES-BOTS. Вопрос был: при kill останавливать контрактных ботов со `stopType 1` (закрыть позицию по рынку)? **Ответ человека ~13:05: «нет, не закрывать при kill-switch».** Kill останавливает всех ботов со `stopType 2`, код `connector.emergency_stop` не меняется, задачи для Insight Executor нет. Вместе с остановкой у контрактного бота снимается его SL. Остаток позиции закрывает надзор, только если цена дошла до бывшего SL бота (`futures-bots.md` §6.5) — это «закрытие по своему стопу» из KILL-FLATTEN-DEFAULT. Худший случай между проверками — маржа бота: плечо ≤ `MAX_LEVERAGE`, маржа ≤ 2% equity. Записано в `risk-core.md` §8 |
| DEMO-USDT-TOPUP | USDT под маржу контрактного флота на demo | OKX Trader | ready | — | Разовый вызов CCXT `private_post_account_demo_adjust_balance` (+5 000 USDT, только demo) через `src.connector.create_exchange(load_settings("demo"))`, без кода в `src/`. Рост USDT подтверждён `okx --demo account balance`. Скачок equity записан в `futures-bots.md` §11 и как info в `ops/incidents.md`. Тип записи в bills записан для `pnl_ledger` (капитал, а не PnL) | Решение оркестратора 12:51: вместо FLEET-USDT-CAP (продажа BTC ждёт P1-72H и GRID-VERDICT) — пополнение demo без влияния на рынок. Над полом 30% свободно ≈ 1.1k USDT, двум волнам нужно ≈ 3.9k. На P1-72H не влияет: скачок equity вверх breaker не трогает. Лимиты OKX: до 5 000 USDT за вызов, 3 вызова в сутки |
| CFLEET-DEMO | Контрактный demo-флот, волны 1 и 2 | OKX Trader | ready | CFLEET-PROBE, DEMO-USDT-TOPUP | По `futures-bots.md` §6: режим рынка пересчитан при запуске; K1–K3, через ≥ 24 ч без инцидентов — K4–K5. Плечо ≤ 2x, бот ≤ 2% equity, сумма маржи ≤ 4%, сумма плановых худших убытков ≤ 0.8%; у каждого SL, `liqPx` за SL по §3.3; `allowReinvest=false`, `basePos` явно. В §11 по каждому боту — algoId, параметры, liqPx, SL, худший убыток; состояние подтверждено повторным запросом | Решение человека ~12:15: «можно их тоже вносить». Проверить 55301 на XRP- и AVAX-SWAP. В `ops/sleeves.json` боты с `algoClOrdId` флота → `demo_fleet`. После kill позиции не закрываются (KILL-CONTRACT-STOPTYPE) — правило §6.5 |
| MON-CFLEET | Надзор за контрактным флотом | Ops Sentinel | ready | CFLEET-DEMO | По каждому боту: state, totalPnl, liqPx и дистанция до неё, mgnRatio, adl, fundingFee, cancelType — в `futures-bots.md` §11; стоп-условия §6.5 применены; задача переставлена на +6 ч | После первого запуска перевести в `scheduled`. Для ботов в `no_close_position` после kill — правило §6.5 |
| CFLEET-VERDICT | Вердикт контрактного флота за 7 дней | OKX Trader | ready | CFLEET-DEMO | Таблица по §6.6 `futures-bots.md`; A/B: K4 против спот-грида SOL, K2 против спот-DCA; keep или kill, проигравшие остановлены; рекомендации по рукавам (live решает только человек) | После запуска флота поставить `scheduled` на дату запуска + 7 дней |
| FLEET-KILL-CGRID-DEMO | Остановка contract_grid кодом на demo | Insight Executor | ready | ACCT-MODE-DERIV | Тестовый contract grid остановлен `stop_grid_bots(algo_ids=[…])`; итоговое состояние подтверждено повторным запросом; остаток позиции закрыт; флот не тронут | Из HUNT-FUTURES-BOTS; удобно объединить с FLEET-KILL-CDCA-DEMO и CFLEET-PROBE. `_list_bots` пропускает `no_close_position` при `stopType 2` (`connector.py:317`) |
| EQUITY-CBOTS | Equity риск-ядра с контрактными ботами | Insight Executor | ready | CFLEET-PROBE | Если маржа ботов вне `totalEq`: equity движка и live-runner = `totalEq` + сумма `eq` ботов или источник, подтверждённый на demo; тест «бот на 4% equity не выбивает дневной breaker»; тесты зелёные. Если маржа внутри `totalEq` — закрыть записью факта | Иначе ложная просадка для `risk.update_equity` (дневной −6%). `engine.py` заморожен до ENGINE-RESTART |
| RISK-SWAP-CORE | SWAP и боты в риск-ядре (только ужесточение) | Insight Executor | ready | RISK-PNL-DOUBLE | В `src/risk.py`: `risk.check_leverage` (потолок `MAX_LEVERAGE`, RISK-LEVERAGE-CAP) вызывается везде, где код задаёт плечо; отказ SWAP-ордеру кода не в isolated; маржа и плановый худший убыток ботов учитываются в heat; SL ↔ liqPx через `validate_stop_vs_liquidation`; тесты и `risk_selftest` зелёные; существующие лимиты не ослаблены | Из HUNT-FUTURES-BOTS. Только ужесточение (AGENTS.md §2). Потолок плеча и его защиту в guard сделала RISK-LEVERAGE-CAP. Funding — RISK-FUNDING-PNL |
| RISK-LEVERAGE-CAP | Потолок плеча в риск-ядре и guard | Claude Code | done | — | `MAX_LEVERAGE` в `src/risk.py` и `check_leverage`: плечо от 1 до потолка, иначе отказ с причиной; константа в `RISK_LIMITS_UP_IS_RISKIER` guard; тесты и `risk_selftest` зелёные; `risk-core.md` §2.2 и роль OKX Trader обновлены | Решение человека ~13:05: «добавь максимально нормальное плечо сам». Выбрано 3x: инвариант `business-plan.md` §7 (≤ 3x, только isolated) и `futures-bots.md` §6; первая волна demo-флота — ≤ 2x. Файлы: `src/risk.py`, `ops/hooks/guard.py`, `tests/test_guard.py`, `tests/test_risk_leverage.py`, `insights/risk-core.md`, роль OKX Trader. **Готово 13:10.** `MAX_LEVERAGE = 3` и `check_leverage` в `src/risk.py` — только добавления (+22 строки), прежние лимиты и `EQUITY_MAX_AGE_S` не менялись. Guard: `MAX_LEVERAGE` в `RISK_LIMITS_UP_IS_RISKIER`. Контроль: без правки guard пропускал 3 → 10 и подмену `risk.MAX_LEVERAGE`, с правкой отклоняет, 3 → 2 проходит. Тесты: `tests/test_risk_leverage.py` (4) и `test_max_leverage_cannot_be_loosened`; `test_guard` с `test_risk_leverage` 20/20, `risk_selftest` 26/26; полный набор на чистой копии ветки с этой правкой 463/463 OK. Кодом плечо пока нигде не задаётся: `check_leverage` подключат RISK-SWAP-CORE и NATIVE-CBOT-WRAP. `risk-core.md` §2.2 и роль OKX Trader (`.github` и `.claude`) обновлены |
| NATIVE-CBOT-WRAP | Контрактные боты через код | Insight Executor | ready | NATIVE-GRID-WRAP, CFLEET-PROBE, RISK-SWAP-CORE, KILL-CONTRACT-STOPTYPE | `contract_grid` и `contract_dca` создаются кодом только при гейте режима рынка §2; плечо ≤ лимита; SL обязателен (neutral — `slRatio`); плечо — через `risk.check_leverage`; проверка ликвидации §3.3; `min-investment` и `grid-quantity`; `basePos` явно, `allowReinvest=false`; стоп-триггер с `delaySeconds`; `algoClOrdId` владельца; маржа ≤ лимита `live-pocket.json`; тесты на фейковой бирже; прогон на demo | В live контрактные боты создаёт только этот код, не CLI агента |
| PNL-LEDGER-CBOTS | Контрактные боты в учёте PnL | Insight Executor | ready | CFLEET-PROBE | Bills контрактных ботов найдены (основной счёт или счёт бота); открытые позиции переоцениваются по `upl`; `fundingFee` и `fee` — из bot API; атрибуция через `ops/sleeves.json`; тесты зелёные; дневной отчёт с контрактным ботом на demo | Сейчас SWAP по mark не переоценивается (`pnl-ledger.md` §5 п. 3) |
| ERR-BOT-CODES | Коды ошибок ботов в ERROR_MAP | Insight Executor | done | ERR-ACCT-SWITCH | `explain_error` возвращает действие для 51055, 51057, 51313, 51340, 51343, 51344, 51348, 51349, 51370, 51380, 51381, 51398, 51399 (`futures-bots.md` §1.7); тесты зелёные | Из HUNT-FUTURES-BOTS. `51070` уже делает ERR-ACCT-SWITCH, поэтому задача после неё — один файл `src/errors.py`. 13:07 отдана тому же субагенту, что и ERR-ACCT-SWITCH, — сразу после неё. **Готово 13:16, принято оркестратором.** `src/errors.py`: 13 кодов ботов по `futures-bots.md` §1.7. У 51343, 51349 и 51370 в таблице нет действия — стоит консервативное «не повторять, проверить параметр, при повторе — задача на доску»; уточнить при первом срабатывании на demo. Проверка — вместе с ERR-ACCT-SWITCH |
| HUNT-SIGNAL-BOT | Signal bot OKX как исполнитель сигнальной стратегии | Crypto Insight Hunter | ready | MEANREV-CALIB | `futures-bots.md` §4 С6 дополнен: формат webhook по ≥ 2 источникам, что происходит с позициями после `stop-order-algo`, режим маржи, план проверки на demo, требования к kill-switch | Низкий приоритет: сигнальной стратегии, прошедшей гейт, пока нет; альтернативный источник сигналов — TREND-DESIGN |

## Архив

| ID | Задача | Агент | Статус | Зависит от | Критерий готовности | Заметки |
| --- | --- | --- | --- | --- | --- | --- |
| P0-SMOKE | Smoke-тест demo | Insight Executor | done | — | place/cancel в demo | `python -m src.smoke_test` |
| RISK-CORE | Риск-ядро `src/risk.py` | Insight Executor | done | — | самотест 21 проверка | + breaker'ы по equity (tests/test_risk_equity.py) |
| KILL-DEMO | Kill-switch на demo | Insight Executor | done | — | отменены лимитный и algo-ордер | grid-боты — только unit-тест |
| EXPTIME | `expTime` в HTTP-заголовке | Insight Executor | done | — | просроченный отвергнут `sCode 50036` | |
| P1-DCA-DEMO | DCA-бот на demo | Insight Executor | done | — | 2 покупки, восстановление после рестарта | `src/dca_bot.py` |
| GRID-START | Запуск нативного grid-бота на demo | OKX Trader | done | — | бот running | `insights/grid-bot-demo-run.md` |
| AGENTS-V2 | Автономность агентов: регламент, доска, guard, автопилот, Ops Sentinel | Claude Code | done | — | тесты guard и автопилота зелёные | `AGENTS.md`, `ops/` |
